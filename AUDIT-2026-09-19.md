# FULL AUDIT — 2026-09-19

**Branch `diag/full-audit`, cut from `main` @ `f7cc8894`. DIAGNOSIS ONLY — this
branch changes no behaviour and fixes nothing.** Every fix is listed at the
bottom, ranked, sized, one PR each.

---

## The four questions, answered before the table

| | Sam's question | answer |
|---|---|---|
| 1 | Does the site publish, on schedule, without a human? | **Yes — and nothing checks it any more.** Zero scheduled runs were dropped in 29 hours. But `collect.yml` has been **red on every single run for 32 hours** and its test gate is `continue-on-error`, so the data ships regardless. What stopped is the guarantee that it was checked first. |
| 2 | Is what it publishes TRUE? | **The MLB card is true to every rule in `CLAUDE.md` — and its confidence numbers are not calibrated.** 290 of 580 graded pitcher rows = **exactly 50.0%**, against advertised 58–93%. That is disclosed on the page and guarded by nothing. Separately, **one published football card was rewritten 15 hours after kickoff**, ten of its prices changed. |
| 3 | Would we KNOW if it stopped being true? | **No.** The watchdog reads a JSON key the writer does not emit, so it has **never once** reported staleness — it says `healthy: true` right now while the freshness contract says NFL is failing. And the one thing that can summon a repair agent asks GitHub for issues **without the permission to read them**, sees zero, and stands down. |
| 4 | Are we inside both budgets? | **Yes, measured on the APIs' own counters.** Odds API **408 credits/day** by the balance chain (13.3k of 20k projected). CFBD **906/month** against the 3,000 plan now deployed. |

🔴 **13 BROKEN · 6 WORKING-BUT-UNGUARDED · 1 UNVERIFIABLE · 17 WORKING.** Four of the BROKEN ones are conditions that cannot report themselves — which is how they have lasted between 32 and 47 hours with nobody told.

---

## 1 · Verdict table

| # | area | question | verdict | evidence |
|---|---|---|---|---|
| 1 | A | is `collect.yml` green? | **BROKEN** | 94 runs, **0 success**, 68 failure, 26 cancelled over 29h; last green run `35286898744` at **2026-09-17T23:27:06Z** |
| 2 | A | how many reds, and how many causes? | **BROKEN** | **at least three, and the count is UNVERIFIABLE**: run `35215321245` failed at `::error::at least one converge pass failed` with `tests green`; every later red sampled failed at `::error::the test suite failed` |
| 2b | A | can a second failure be seen behind the first? | **BROKEN** | `collect.yml:1194` (`Fail if the tests failed`) precedes `collect.yml:1200` (`Fail if any pass failed`), so while the suite is red **every converge failure is skipped and invisible** |
| 3 | A | why does `test_watch_label.py` pass here and die in CI? | **BROKEN** | CI Tests step: `⛔ could not extract the decide step: ModuleNotFoundError: No module named 'yaml'` → `❌ 10 of 41 checks FAILED` |
| 4 | A | are the 26 cancelled runs a fault? | **WORKING** | all in the `collect-mlb` group; `collect.yml:411` `cancel-in-progress: true`, deliberate (*"a freshly landed run REPLACES the loop"*) |
| 5 | A | what does the red cost per run? | **WORKING-BUT-UNGUARDED** | `Tests 14:11:37 → 14:23:50` = **12m13s**, 600s of it the vacuity timeout, 24×/day; `vacuity.yml` runs the same sweep again nightly |
| 6 | B | is CRON TOTAL still 55? | **WORKING** | 55 counted per file out of the workflow sources |
| 7 | B | are the declared crons actually firing? | **WORKING** | expected 114 fires in the window, **observed 115**, drop rate ≈ 0% |
| 8 | B | do the routing branches all reach a mode? | **WORKING** | no observed cron label is missing from the routing table and none claims a schedule that does not exist |
| 9 | C | does the whole suite pass locally? | **WORKING** | 99 files (97 py + 2 js; `test_vacuity.py` deferred and run isolated): `FAILED: none  TIMEOUT: none` |
| 10 | C | does the vacuity harness bite? | **WORKING** | isolated sweep, **18m16s, exit 0, 40/40 checks**, tree verifiably clean, no leaked worktree |
| 11 | C | are the two known VACUOUS guards still vacuous? | **BROKEN** | sweep returns `('nfl.py','VACUOUS')` twice — `test_accumulators.py`, `test_possession.py`, open on issue #69 since 2026-09-18 |
| 12 | C | how many guards has nothing ever tried to break? | **WORKING-BUT-UNGUARDED** | blind set **56 of 98** test files, ceiling 56, **not grown** — the ratchet holds but the backlog is 57% of the suite |
| 13 | C | can a test die in CI for a missing module without saying so? | **WORKING-BUT-UNGUARDED** | 3 files import `yaml`, 5 import `playwright`; **7 of 8 degrade to a `note()`**, `test_watch_label.py` is the one that hard-fails |
| 14 | D | is the MLB card true to its own rules? | **WORKING** | 15 independent checks on `picks/2026-09-18.json`: descending ✅, `blend` = 50/50 ✅, top-10 gate −400 ✅, 24 parlays 0 same-game ✅, 8 pairs all ≥ 1.80 ✅, 0 jargon ✅ |
| 15 | D | is only what the reader can ACT on marked actionable? | **WORKING** | 34 flags on rows, **6 actionable, all of them** *"Hard Rock didn't post this one"*; T21/T22/rule 15/STEP 4B still written, none displayed |
| 16 | D | does the Track Record reconcile? | **WORKING** | reproduced independently: `by_day`, `by_kind`, `by_side`, `by_market` all sum to **729/1195 = 61.0%**; `verify_record.py` re-grades and agrees |
| 17 | D | are the published confidence numbers calibrated? | **BROKEN** | `by_kind.pitcher = {'w': 290, 'n': 580, 'pct': 50.0}`; bands deliver −10.0, −14.7, −14.3 and −42.4 points against their claims |
| 18 | D | is a published card ever rewritten after its games start? | **BROKEN** | `picks/fb-nfl-2026-09-17.json`, kickoff **00:15Z**, last rewritten **2026-09-18T15:52:33Z** — 10 prices changed, 12 rows swapped |
| 19 | D | do the card's own notes describe the card it ships? | **BROKEN** | doc-level `hitter_note` says *"Hitter rows carry NO confidence rating"* on a card whose 25 hitter rows carry `confidence`; `projection_note` describes an inversion **retired 2026-08-27** |
| 20 | D | why is NFL `fb-scores` 3.5h stale? | **BROKEN** | `data/nfl/latest/schedule-probe-2026.json`: `"error": "HTTPError: HTTP Error 403: rate limit exceeded"` |
| 21 | E | do the stored artifacts exist and parse? | **WORKING** | **2,368** JSON/JSON.GZ under `data/` and **44** cards under `picks/`: **0 fail to parse** |
| 22 | E | are the dated archives write-once? | **WORKING** | over the last 400 commits, **1** dated path written twice — `data/2026-09-17/results/final.json.gz`, which is not a `daystore` path; every `daystore` path once |
| 23 | E | does any contract pass by NOT ASKING? | **BROKEN** | `watchdog.py:642` reads `f.get("rows")`; `freshness.json` is written with the key **`artifacts`** (`collect.py:4851`) — the staleness check has **always** seen an empty list |
| 24 | E | is the NFL possession artifact being built? | **BROKEN** | `nfl/latest/top-probe-2026.json` **never committed**; `nfl-logs` last succeeded **2026-09-17T07:34:19Z**, 30 min before its writer landed |
| 25 | F | does the MLB card still verify? | **WORKING** | `verify_card.py`, `verify_board.py`, `verify_record.py` all pass; **MLB read-only, nothing touched** |
| 26 | F | is the MLB freshness contract met? | **WORKING** | `PASS — everything the page depends on is inside contract`, 13 of 13 rows ok |
| 27 | G | Odds API, measured on its own counter? | **WORKING** | 304 stored `credits_remaining` readings: balance chain **408 credits/day**, Sept MTD 19,994 → 12,034 = 442/day ⇒ ~13.3k of 20k |
| 28 | G | CFBD, measured on its own counter? | **WORKING** | `MEASURED, from CFBD's own header (1 reading)` highest remaining **2,104**; 906/month = **30%** of the deployed 3,000 plan |
| 29 | G | does `CFBD_PLAN: "3000"` reach every workflow that needs it? | **WORKING** | only `cfbd.yml` runs a CFBD budget program, and it sets it (`cfbd.yml:87`) |
| 30 | G | is the repository growing dangerously? | **WORKING** | pack **105.98 MiB**, +5.37 MB/day packed, **~179 days to 1 GiB** |
| 31 | H | do the project docs agree with the code? | **UNVERIFIABLE** | all seven named docs are **absent from this repository** — exactly as `CLAUDE.md` §2 predicts |
| 32 | H | is `CLAUDE.md`'s own file map accurate? | **WORKING-BUT-UNGUARDED** | `test_claude_md_map.py` passes 9/9 but states it *"does not claim the one-line description beside each module is accurate"* |
| 33 | ALL | can the repo turn any of this into somebody's task? | **BROKEN** | self-repair triage has `permissions: contents/pull-requests: read` and **no `issues:` scope**; deployed run `35424213172` printed `open watcher issues: 0` while labelled issue #80 was open |
| 34 | ALL | do the watcher issues carry the label triage reads? | **BROKEN** | **1 of 6** open watcher issues has `gizmo-watch`; the 10 `gh issue edit` update-in-place sites never add it |
| 35 | ALL | does an alert ever assert a fact it did not measure? | **WORKING-BUT-UNGUARDED** | `calibration.py:168` and `budget_watch.py:258` both print *"every job is green"* as a literal, while `collect` is red on every run |
| 36 | ALL | is every module reachable from something that runs? | **WORKING-BUT-UNGUARDED** | `verify_nfl.py` — *"caught four real defects, two before Sam saw them"* — is invoked by **no workflow and no test**. Rule 78. |

---

## 2 · The evidence, as commands and their output

### 🔴 F1 + F2 · `collect.yml` is red on every run, for at least three reasons

```
$ aggregate of 300 runs, window 2026-09-18T02:46:05Z -> 2026-09-19T07:54:02Z
workflow                 total  success    failure cancelled
collect.yml                 94        0         68        26
browser.yml                  6        6          0         0
budget / calibration / cfbd / owed_tests / runs / self-repair / vacuity   all green
```
Last green collect run: `35286898744`, **2026-09-17T23:27:06Z** — 32 hours ago.

⚠️ **I set out to group all 68 by failing step and cannot — and the reason is
itself a finding.** There are two terminal gates and they are ordered:

```yaml
1194:  - name: Fail if the tests failed
1195:    if: steps.tests.outputs.rc != '0' && steps.tests.outputs.rc != ''
1200:  - name: Fail if any pass failed
1201:    if: steps.collect.outputs.rc != '0'
```

The tests gate runs **first** and `exit 1`s, so from the moment the suite went
red the converge gate has been **skipped on every run** — a collector failure
cannot reach the Actions page at all while this lasts. Confirmed on job
`105631080959`, where both gates read `skipped`.

What the logs do bound:

| cause | evidence |
|---|---|
| **a converge pass** | run `35215321245`, 2026-09-17T11:22 — `tests green; nothing to report`, then `##[error]at least one converge pass failed` |
| **`test_vacuity.py(TIMEOUT)`** | run `35303516695`, 2026-09-18T03:31 — `NAMES='test_vacuity.py(TIMEOUT)'` |
| **`+ test_watch_label.py`** | issue #80, 2026-09-19T01:45 — `` Failing file(s): `test_vacuity.py(TIMEOUT),test_watch_label.py` `` |

⛔ **How many of the 68 are converge failures hiding behind a red suite is
UNVERIFIABLE from here.** **F24 (nflverse 403, 47 hours) is at least one of
them**, and it is the shape of whatever else is in there.

⚠️ **The data still ships.** `Tests` carries
`continue-on-error: ${{ github.event_name != 'push' }}`, so the collector runs,
commits and the page updates. What is gone is the guarantee that the numbers
were checked before they shipped.

### F4 · The 26 cancelled runs are the design, not a fault

All 26 are in the `collect-mlb` concurrency group (17 scheduled + 9 push).
`collect.yml:411` sets `cancel-in-progress: true`, and the comment above it says
why: *"a freshly landed run REPLACES the loop that is already holding the
line."* Job `105631080959` is the pattern — `Converge and hold` ran
14:23:50 → 15:19:19 and was replaced by the next run in the group.
⛔ **Not a finding.** Recorded because 26 cancelled runs look like one.

### F6–F8 · The schedule itself is healthy

```
$ CRON TOTAL, counted from `^\s*-\s*cron:` per file
browser 2 · budget 1 · calibration 1 · cfbd 1 · collect 43
owed_tests 1 · runs 1 · self-repair 4 · vacuity 1        = 55

$ expected fires over the window, from the cron expressions, vs the run list
TOTAL expected 114, observed 115, drop rate -0.9%
```
The +1 is `runs.yml 41 * * * *` straddling the window edge. 16 declared crons did
not fire because they are day-of-week arms outside a Fri→Sat window; no observed
cron label is missing from the routing table.
⚠️ **29 hours is a short window.** `CLAUDE.md`'s historic *"only 29 of 70
gamelines hour-slots produced a file"* is not reproducing today, and one clean
day does not retire that warning.

### 🔴 F33 · The repair channel asks GitHub a question it is not allowed to ask

`self-repair.yml`'s `triage` job:
```yaml
    permissions:
      contents: read
      pull-requests: read          # ← no `issues:` scope
```
and its gate:
```sh
QUEUE=$(gh issue list --state open --label gizmo-watch ... 2>/dev/null || echo "[]")
```
The deployed run, `35424213172` (2026-09-19T05:32:22Z):
```
unrepairable findings: 0
open watcher issues: 0
nothing unrepairable and no watcher issue open
```
At that moment issue **#80** was open and carried the label. Asked with a token
that can read issues, the same query returns it:
```
$ list_issues state=OPEN labels=["gizmo-watch"]     →  totalCount: 1   (#80)
```
⚠️ **Both halves of the line destroy the evidence**: `2>/dev/null` swallows the
403 and `|| echo "[]"` turns "denied" into "nothing to do". Whatever the cause,
the step cannot tell those two apart, and **that is the finding** — the alert
that could tell us is the one that was silenced.

⛔ **`test_watch_label.py` cannot catch this and never could.** Sections 1–5 read
the workflow as text; §6 drives it with a stub `gh` on `PATH`, which has every
permission in the world. **Nothing in this repo reads a job's `permissions:`
block.**

➡️ The entire point of PR #73 was to turn a red test, a failed run and a vacuous
guard into somebody's task. **It has produced nothing since it merged.** The `fix`
job read `skipped` on run `35424213172`; `data/latest/self-repair-last.json` — which
the recorder writes on every pass that summons the agent — does not exist; and
`git ls-remote --heads origin` shows no `self-repair/*` branch.

### 🔴 F34 · Five of six watcher issues cannot be seen even with the permission

| issue | what it watches | label |
|---|---|---|
| #80 | the test suite is failing | `gizmo-watch` ✅ |
| #69 | two guards are not guarding anything | — |
| #44 | a workflow is failing | — |
| #42 | T58/T59 accumulating | — |
| #31 | CFBD past the free tier | — |
| #16 | the board is not delivering what it claims | — |

Every watcher files once and then **updates in place**. There are 10 such sites:
```
$ grep -rn "issue edit" .github/workflows/*.yml | wc -l
10
```
and **not one of them adds a label**. PR #73 labelled the `gh issue create` path;
every issue that already existed is invisible to triage for ever. ⚠️ **Issue #69
— the two vacuous guards, the exact thing PR #73 was written to make actionable —
is one of them.**

### 🔴 F23 · The watchdog's staleness check has never once run

```python
# watchdog.py:642
stale = [r for r in (f.get("rows") or []) if r.get("stale")]
```
```
$ python3 -c "import json,io; d=json.load(io.open('data/nfl/latest/freshness.json')); ..."
top-level keys: ['artifacts', 'built_at', 'card_blocked', 'card_caveat', 'kind', 'note', 'ok', 'written_at']
has "rows"? False | ok = False
artifacts: 7 rows, row keys: ['age_min','due_at','due_et','kind','late_min','missing','mode','paid','path','stale','why']
rows with stale=True: 1 -> ['nfl-logs']
watchdog would see: 0 stale rows
```
The writer is `collect.py:4851` — `"artifacts": rows`. The reader asks for
`rows`. So `check_freshness()` `continue`s on every league on every pass, and
the result is the headline:
```
$ python3 -c "print(json.load(open('data/latest/health.json'))['healthy'])"
True                        # 0 findings, 0 unrepairable
$ LEAGUE=nfl python3 verify_freshness.py
::error::nfl-logs MISSED its 3:00 ET build: never built
::error::fb-scores MISSED its 4:00 ET build: last built 212m ago, 19m past due
FAIL — 2 artifact(s) past due
```
⚠️ **Two graders of one question disagree, and the one that summons the repair
agent is the one saying healthy.** Rule 67, shipped and live. There is a second
bug behind it: even fixed, the message reads `r.get("key")` and the rows carry
`mode`, so it would print `None tabs are showing…`.

### 🔴 F24 + F20 · nflverse has been answering 403 for 47 hours

```
$ cat data/nfl/latest/backfill-report.txt
nfl-logs back-fill at 2026-09-19T07:38:29Z
requested: [2026]
written  : []
failed   : [2026]
--- 2026 ---
HTTPError: HTTP Error 403: rate limit exceeded
```
```
$ git log -1 --format='%ad' -- data/nfl/latest/allowed-by-position-2026.json.gz
2026-09-17T07:34:19+00:00          # the last nfl-logs that wrote anything
$ git log --oneline --all -- 'data/nfl/latest/top-probe-*'
                                    # never committed, ever
```
The possession writer (`collect.py:4346`) landed on `main` with PR #39 at
**2026-09-17T08:04:10Z** — 30 minutes *after* the last successful `nfl-logs`.
So the artifact the NFL dossier's §6 needs has never existed, and the same 403
is why NFL `fb-scores` is 3.5h stale while college's is 25m:
```
$ cat data/nfl/latest/schedule-probe-2026.json
{"season": 2026, "kind": "DIAGNOSTIC", "usable": false,
 "error": "HTTPError: HTTP Error 403: rate limit exceeded", ...}
```
✅ The collector **does** fail loud (`raise RuntimeError` after the report), and
`freshness` **does** say so. ⛔ But the run was already red for the test suite, so
a two-day data-source outage has been landing inside a red that everybody has
learned to ignore — and the watchdog that would have escalated it is F23.

### 🔴 F18 · A published card was rewritten 15 hours after kickoff

```
$ git log --format='%ad' -- picks/fb-nfl-2026-09-17.json | head -3
2026-09-18T15:52:33+00:00
2026-09-18T15:33:53+00:00
2026-09-18T13:57:33+00:00           # 15 commits in total, 6 of them post-kickoff
$ python3 -c "...earliest kickoff..."
n rows 25 earliest kickoff 2026-09-18T00:15:00Z
```
Diffing the first published version against the last:
```
first 2026-09-17T11:28:49+00:00 rows=25 | last 2026-09-18T15:52:33+00:00 rows=25
CHANGED after kickoff: 10
   ('Keon Coleman', 'player_receptions', 'over', 1.5)         (158, 95) -> (145, 95)
   ('Dawson Knox', 'player_reception_yds', 'over', 12.5)      (-113, 89) -> (-120, 89)
   ('Jared Goff', 'player_rush_yds', 'under', 0.5)            (-175, 64) -> (-185, 64)
   ... 7 more
dropped: 12 added: 12
```
`CLAUDE.md`: *"Published estimates are a permanent record. ⛔ Never edit or
delete one after its games have started."* ⚠️ **MLB obeys it** — no
`picks/<date>.json` appears in the rewrite list at all. The mechanism is in
`card.py`, which **returns `None` once every game on the board has started**
(`verify_card.py:9`, and it printed exactly that for me today). ⛔ **`card_fb.py`
has no such stop** — 17 mentions of `commence`/`kickoff` and not one of them
compares a kickoff to now. `record_fb.py` and `shadow_fb.py` grade against
these files.

### 🔴 F17 · The MLB pitcher model is a coin flip, and says 58–93%

```
$ python3 -c "print(json.load(open('data/latest/record.json'))['by_kind'])"
{'pitcher': {'w': 290, 'n': 580, 'pct': 50.0}, 'hitter': {'w': 439, 'n': 615, 'pct': 71.4}}
```
```
calibration: [{"bucket":"50-60%","predicted":56.4,"n":181,"pct":46.4},
              {"bucket":"60-70%","predicted":64.2,"n":279,"pct":49.5},
              {"bucket":"70-80%","predicted":73.5,"n":98,"pct":59.2},
              {"bucket":"80-90%","predicted":85.5,"n":17,"pct":47.1}]
```
✅ **The page says so, in the reader's own words** — `index.html:1702` renders
`calibration_warning` as *"Read the confidence number honestly."* That is the
design working.
⛔ **Nothing alarms on it.** `calibration.py` ends its own report *"Football only
— MLB is frozen and is never read here."* So the one number the whole product
exists to produce has **no automatic watcher at all**, and the only reason we
know is that the card writes its own confession.
⚠️ **MLB IS FROZEN. This is reported, not touched, and no fix for it appears in
the list below.** It needs Sam and the ledger, not a PR.

### F19 · The card's own notes describe a card it stopped shipping

```
hitter_note      "Hitter rows carry NO confidence rating and NO band..."
```
…on a card where all 25 hitter rows carry `confidence` and `confidence_basis: RECORD`
(`CLAUDE.md` records the change on 2026-08-24).
```
projection_note  "...inverted through the same distribution that row assumes..."
```
…describing an inversion **retired 2026-08-27, ledger rule 66**.
⚠️ **Neither is rendered** — `index.html` uses the *row-level* notes, which are
correct and reader-facing. So this is not a page falsehood; it is the permanent
record describing its own columns wrongly. **MLB is frozen: reported only.**

### F3 · Why my own test file has been red in CI since it merged

```
═══ 6. 🔴🔴 AND THE GATE IS DRIVEN, NOT READ ═══
  ⚪ ⛔ could not extract the decide step: ModuleNotFoundError: No module named 'yaml'
  ❌ ⚠️ the `decide` step was extracted from the deployed workflow
  ...
❌ 10 of 41 checks FAILED
```
PyYAML is not installed on the runner and no workflow installs it. **The repo
already knew**: `test_multileague.py:82` and `test_watchdog.py:328` both say so
in as many words, and both keep asking their question with a hand parser.
⛔ **The guard I wrote to stop §6 silently checking nothing did its job** — it
went red rather than green-on-nothing. It is the *drive* that is unrunnable, not
the guard.

### F5 · What the red costs

```
Tests                14:11:37 → 14:23:50     (12m13s)
Converge and hold    14:23:50 → 15:19:19     (cancelled by the next run in the group)
```
600s of those 12 minutes is `test_vacuity.py` running out its clock. Measured
here, isolated: the sweep takes **18m16s** and passes 40/40. It is ~3× the
budget, and `vacuity.yml` already runs the identical sweep nightly.

### The things that are fine, measured rather than assumed

```
$ for lg in mlb nfl ncaaf; do LEAGUE=$lg python3 verify_freshness.py; done
mlb   : PASS — everything the page depends on is inside contract   (13/13)
ncaaf : FAIL — 1 artifact(s) past due   (fb-scores, 19m, clears on the next pass)
nfl   : FAIL — 2 artifact(s) past due   (F24 above)

$ python3 verify_card.py && python3 verify_board.py && python3 verify_record.py
... ALL BOARD CHECKS PASSED
... THE TRACK RECORD RECONCILES

$ suite, the way collect.yml runs it, vacuity deferred
ran 99 file(s)   FAILED: none   TIMEOUT: none

$ python3 test_vacuity.py
✅ all 40 checks passed        18m16s, exit 0, tree clean, no leaked worktree
   tier 2: 137 declared mutations · tier 1: 20 pairs · blind set 56 of 98 (ceiling 56)

$ python3 budget.py
TOTAL (measured)  13710/month of 20,000  (69%)   ✅ FITS

$ balance chain over 304 stored credits_remaining readings
mean 408/day over 26 chained days      Sept MTD 19,994 → 12,034 (442/day)

$ CFBD_PLAN=3000 python3 cfbd_budget.py
✅ 906/month against a 3000 plan — 30%

$ python3 repo_watch.py
pack 105.98 MiB · +5.37 MB/day · ~179 days to 1 GiB
```

---

## 3 · How this audit could have produced a false clean bill, and what I did

| the trap | what I did |
|---|---|
| Reading a watcher's output instead of measuring | `health.json` says `healthy: true`. I did not believe it — I ran `verify_freshness.py` myself, which is how F23 surfaced. |
| Running a check that has nothing to check | `verify_card.py` printed *"No card to verify — every game on the board has started"*. **That is not a pass.** I audited `picks/2026-09-18.json` directly, 15 checks, by hand. |
| Trusting a test because it is green here | The full suite is green **locally** and has been red in CI for 32 hours. I went to the CI log for the Tests step, not to the issue body that summarises it. |
| Believing a red is noise because everything is red | 68 reds turned out to be **at least three** causes, and a 47-hour data outage (F24) is hiding inside them — behind a gate that skips the one that would say so. |
| Counting a `cancelled` run as a failure | 26 cancelled runs are `cancel-in-progress` working as designed. Read the concurrency comment before calling it. |
| Trusting my own earlier report | `test_watch_label.py` is mine, I reported it green, and it has never passed in CI. It is finding #3. |
| Believing a derived budget | Re-derived Odds spend from **304 stored `credits_remaining` readings**, not from `budget.py`'s model. They agree to within 12% (457/day modelled vs 408/day on the counter). |
| Reading file mtimes | Every "when did this last change" here comes from `git log` or from the artifact's own stamp, never from the filesystem. |
| A grep that matches prose | `CRON TOTAL` counted from `^\s*-\s*cron:` only; `verify_nfl.py`'s four references all turned out to be comments, which is *why* F36 is a finding. |
| Declaring something missing on one bad command | `teams.json` is absent for NFL — and the page never asks for it (`fbTeamsLoad()` uses `nflDirectory()`). **Not a finding.** Checked before writing. |

---

## 4 · The ranked fix list

**One PR per cause. Every one ships a guard driven red before it is believed.
None of them is started on this branch.**

| # | fix | size | guard, and the CLASS it guards |
|---|---|---|---|
| 1 | **Grant the `triage` job `issues: read`, and make the query fail loud instead of `\|\| echo "[]"`** | **S** — 2 lines of YAML + an error path | **CLASS:** *a job calling `gh <noun>` without the matching `permissions:` scope.* Derive every `gh` noun used in every job from the workflow source and require the scope. ⛔ Today **nothing reads a `permissions:` block**, which is why §6's nine drives could not see this. ⚠️ `cron:` file → **PR, never a push.** |
| 2 | **Add `--add-label gizmo-watch` to all 10 `gh issue edit` sites** | **S** | **CLASS:** *every path that can put an issue into the queue must label it, not only the create path.* `test_watch_label.py` already derives the create sites; extend it to the edit sites and drive it red by removing one. ⚠️ `cron:` files → PR. |
| 3 | **`watchdog.py:642` — read `artifacts`, and name the row by `mode`** | **S** — 1 line + the message | **CLASS:** *a reader keying on a field the writer does not emit.* Guard generically: for every repo-written JSON artifact a module reads, assert the keys it reads exist in the real file. Drive it red by renaming `"artifacts"` in `collect.py`. 🔴 **This is the highest-value line in the list** — it is why `healthy: true` and `FAIL` coexist. |
| 4 | **Refuse to rewrite `picks/fb-<lg>-<date>.json` once its first kickoff has passed** | **M** — a guard in `card_fb.py`'s writer | **CLASS:** *a published estimate is immutable after its slate starts, in every league.* MLB gets it for free because `card.py` stops building once the slate starts; football needs it stated, and the guard belongs where both leagues are covered. Drive it red against `picks/fb-nfl-2026-09-17.json`. ⛔ The **10 already-corrupted rows are not to be repaired** — that would be a second edit to a published record. |
| 5 | **Authenticate `nfl.py`'s fetch and back off on 403.** `nfl.py:30` reads `https://api.github.com/repos/nflverse/nflverse-data/releases` with a UA header and no token — an unauthenticated GitHub API call from a shared runner IP. ⚠️ **The token is the likely remedy, not a measured one**: prove the 403 clears before believing it, and ship the back-off either way | **M** | **CLASS:** *any source that rate-limits by IP needs a back-off and a loud, dated report* — `cfb.py` already has one for CFBD's 429 and `nfl.py` has none. Guard: a 403 must produce a `stale` freshness row **and** a non-zero mode exit, driven against a stubbed 403. ⚠️ stdlib only; a token header is stdlib. 💰 Zero new cost. |
| 6 | **Make `test_watch_label.py` §6 extract the `decide` step without PyYAML** | **S** | **CLASS:** *a test may not need a module CI does not install in order to ask its question.* 7 of 8 third-party importers degrade to a `note()`; this one hard-fails. Guard: derive every non-stdlib import in `test_*.py` and require either a graceful fallback or an install step. Drive it red by removing `test_multileague.py`'s `except ImportError`. |
| 7 | **`verify_nfl.py` is wired into something that runs** | **S** | **CLASS (rule 78):** *a verifier nothing invokes runs never.* Guard: every top-level `verify_*.py` must be reachable from a workflow or a test. It will fail on `verify_nfl.py` today — which is the point. |
| 8 | **Both terminal gates report, instead of the first consuming the run** | **S** — `if: always()` on the converge gate, or one gate that names every failing subsystem | **CLASS:** *a gate that exits before a sibling gate can speak.* A run may be red for more than one reason and must say all of them. Guard: drive a run state with BOTH `tests.rc != 0` and `collect.rc != 0` and require both messages. ⚠️ `cron:` file → PR. |
| 9 | **`calibration.py:168` / `budget_watch.py:258` stop asserting "every job is green"** | **S** | **CLASS (rule 166/293):** *a claim about the world written as a literal.* Either measure it (`runs_report.py` already can) or delete the clause. Guard: no watcher body may contain a green/healthy claim it did not compute. |

### ⚠️ Three things that are NOT on that list, deliberately

- **The vacuity sweep's runtime.** Sam said not to touch it, and I have not. ⛔ But
  it is *why* the suite can never go green: the sweep measures **18m16s** against
  a **600s** per-file clock that was set when the slowest file took 115s. That is
  a decision, not a fix — **(a)** give that one file a clock matched to its
  measured cost, or **(b)** let `vacuity.yml`'s nightly job be the only place it
  runs, since it already runs the identical sweep. Both leave the check itself
  untouched. ➡️ **Sam picks.**
- **The two VACUOUS guards** (`test_accumulators.py`, `test_possession.py`). Issue
  #69 has been open since 2026-09-18 and PR #73 recorded them as already three days
  old then; today's sweep still returns `('nfl.py','VACUOUS')` twice. `vacuity.py`
  says *"Sam decides what happens to each."* ⛔ They may only be STRENGTHENED.
- **Everything MLB.** F17 (pitcher rows 290/580 = 50.0% against 58–93% claimed)
  and F19 (two card-level notes describing retired behaviour) are **reported and
  untouched**. ⛔ MLB is frozen; both need the ledger, which is not in this repo.

---

## 5 · What I did NOT change

- **MLB** — `card.py`, `verify_card.py`, the model, the card, the MLB docs. Not in
  this diff. Every MLB check here was **read-only**: three verifiers run, one card
  audited from disk, one record reproduced arithmetically.
- **Any `cron:` line, anywhere.** `CRON TOTAL` = **55** before and after.
- **Any workflow file.** Not one byte.
- **Any `.py`, `index.html`, or any test.** Not one byte.
- **Any `picks/` or `data/` file.** Zero writes; zero API calls of any kind.
- The vacuity sweep ran **isolated**, in its own worktree, and the tree is
  verifiably clean afterwards (`git status --porcelain` empty, `git worktree list`
  shows only the checkout).

**One file added: `AUDIT-2026-09-19.md`. Nothing else is in this diff.**
