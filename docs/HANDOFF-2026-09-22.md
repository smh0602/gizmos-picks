# HANDOFF — from the Cowork session to Claude Code, 2026-09-22

Read `CLAUDE.md` first. This file is what a fresh Claude Code session needs
to pick up exactly where the Cowork session stopped. **It is dated and it
goes stale: check every claim against `main` before acting on it.**

## How work reaches `main` from now on

1. ✅ **Work on a branch. Open a pull request. Never push to `main`.**
2. ✅ **`pr-tests` runs the full suite on the PR.** It is the collector's own
   test loop, line for line (`test_pr_tests.py` enforces that).
3. ✅ **Sam merges only on a green check.** That is the whole point: before
   2026-09-22 the suite ran only after a change was already live.
4. ⛔ **Files with a `cron:` block** (`collect.yml`, `budget.yml`,
   `self-repair.yml`, `browser.yml`, `calibration.yml`, `cfbd.yml`,
   `owed_tests.yml`, `runs.yml`, `vacuity.yml`) **are NOT changed in a PR.**
   Put the new file in the PR description or a comment and tell Sam; he
   uploads it by hand with GitHub's "choose your files" button, into
   `.github/workflows/`. Reason: CLAUDE.md, "IF YOU CAN PUSH TO THIS REPO".
5. ⛔ **One agent at a time.** Do not work while another Claude session is
   editing this repo, and never while `vacuity.py` is sweeping (it mutates
   source files).
6. ✅ **Sam's instructions style:** click by click, Windows, exact folder
   and verbatim commit text. He only uploads and merges. Nothing that
   updates site data may need a manual run.

## State at handoff (verified on `main`, 2026-09-22 ~08:00Z)

All shipped and verified byte for byte (37 files, commits `11747476` →
`74b7ad66`):

- `push_retry.sh`: the fix for the "dark crons" and the ~50% of unrecorded Odds
  spend. The cause was a push loop that lost whole converge passes.
- Football uses three books (Hard Rock, FanDuel, DraftKings); MLB five.
- Parlay bands are floor-only: 2 legs ≥ 1.8x, 3+ legs ≥ 3x, ranked by joint
  model probability.
- MLB unfrozen; `mlb_tables.py` rebuilds the pitcher/opponent tables.
- `watchdog.check_credit_reconciliation`; the repo-size check is now
  required in CI; freshness rows for record-detail and the MLB tables.
- `docs/project-archive/`: verbatim copies of finished claude.ai project
  docs (moved to stay under the project storage cap).

## Open work, in order

1. **Watch the first runs after `74b7ad66`.** Expect green. A red "could not
   push after 5 attempts" is `push_retry.sh` doing its job (before it, the
   same event was silent). More than once a day means investigate.
2. **T60 cannot pass as written.** It grades BOTH sides of every priced
   line, which pins the pooled rate near 50% against a 55.4% bar. It will
   read FAIL at its n floor, and that verdict says nothing about signals
   1–8. ⛔ Do NOT re-decide T60 (owed-tests rule). This goes to Sam.
3. **T60R: the played-games backtest Sam asked for** — NFL + FBS, 2025 +
   2026 to date, game spreads and totals only. NFL prices come from
   nflverse closing lines; CFB from CFBD `/lines` (~20–25 calls). Props are
   infeasible (no stored 2025 prices). ⛔ **Before touching any outcome
   data:** a DIRECTION RULE (which one side the signals pick per game) must
   be written and Sam must approve it and the bar. Lookahead filters: H2H
   `when < kick`; §4 date cut; §5 rebuilt per game; §7 one-week lag;
   §8 weather blank. Never pool with T60. Once approved, it grades each
   new week automatically (a cron, uploaded by Sam).
4. **`card_fb.py` does not read the dossier.** Signals 1–8 are displayed,
   not used to pick. Wiring them in must wait for a passing backtest.

## The project context: read these, in this order

Sam's reasoning, rules and history live in his claude.ai project, which a
repo session cannot open. **Snapshots of the docs that matter were copied
into this folder on 2026-09-22, word for word:**

| file | what it is |
|---|---|
| `snapshot-betting-project-instructions.md` | the project's source of truth: goals, framework, pre-publish checks, working agreement |
| `snapshot-pick-ledger.md` | every graded pick plus the **numbered standing rules** (rule 28 = parlay bands, 48 = books, 53 = never hide a play, 55 = MODEL/MARKET/DESCRIPTIVE labels, 321–325 = this week) |
| `snapshot-owed-tests.md` | the **pre-registration register**. ⛔ Never re-decide an entry after seeing its data (T60 lives here) |
| `snapshot-card-blueprint.md` | how an MLB card is built, step by step |
| `snapshot-mlb-projection-model.md` | the model and every coefficient (the only place they live) |
| `snapshot-gizmos-picks-spec.md` | the website product spec |
| `snapshot-plan-1-8-completion.md` | football signals 1–8, the T60 finding, the T60R proposal |
| `project-archive/` | finished history (older ledger, old databases) |

⚠️ **They are SNAPSHOTS, dated 2026-09-22, and they go stale.** Two
scheduled tasks in claude.ai (nightly grading, the Monday sweep) keep
writing to the ledger and the register there. For anything after
2026-09-22, or if a snapshot and the code disagree: **believe the code,
and ask Sam** — never infer what a newer doc says.
⛔ **Do not edit the snapshots to record new work.** New decisions go in
the PR description and in a new dated `docs/HANDOFF-*.md`.
