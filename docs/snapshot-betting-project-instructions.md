# Sports Betting & Fantasy Analysis — Project Instructions

**Source of truth.** The Project Instructions settings field points here. Read this before any work.

> ✅ **Path: `claude/betting-project-instructions.md`.** A `project_write` 503 deleted this doc from the project root on 8/20; the API namespaces new bare filenames into `claude/`, so the root path cannot be recreated. **Sam updated the settings box on 8/21 — verified.**

## Docs

- **`claude/betting-project-instructions.md`** (this file) — goal, bet construction, framework, process, revision log.
- 🔴 **`claude/card-blueprint.md`** — ⭐ **THE PROCESS FOR BUILDING A CARD. Read before every card.** Board split · which stats matter · the blend definition · sources · **alt ladders mandatory** · **the payout band (~~1.8x–2.1x~~ — the bands are owned by `claude/pick-ledger.md` rule 28; corrected 2026-09-21) and the ladder-direction rule** · output format · **≥5 pairs.**
- 🔴 **`claude/mlb-projection-model.md`** — ⚠️ **THE MODEL. Authoritative on every number. This file deliberately does not restate the coefficients** — see the drift note below.
- 🔴 **`claude/owed-tests.md`** — **THE PRE-REGISTRATION REGISTER.** Every owed test with its specification and pass criterion **written before it runs**, plus the DOC DEBT list. ⛔ **Never re-decide an entry after seeing the data.**
- **`claude/pick-ledger.md`** — every pick, graded; the money; **the standing rules.**
- 🆕 **`claude/calibration-accumulators.md`** — **the rule-15 scoreboard and the rule-35 shadow ladder.** Both grow one block per slate. **The ledger is the RECORD; this is the DIAGNOSTICS.**
- **`claude/mlb-pitcher-database.md`** · **`claude/mlb-opponent-database.md`** — the model inputs. Free, zero credits. **Read before pricing anything.**
- **`claude/mlb-data-stack.md`** — how to fetch anything, and the traps. **Contains THE RECIPE** — full season rebuild, 340 pitchers, ~7 seconds, zero credits, browser required.
- **`claude/backtest-results.md`** — every fitted result. 🧭 **Also holds the WHERE TO LOOK authority table.** **Start there when cold.**
- **`claude/pitcher-reliability.md`** — CV, tail rate, outs↔K decoupling.
- **`claude/opponent-metric-bug.md`** — the opponent input vs the fitted variable.
- **`claude/pitcher-tier-finding.md`** — GOOD / BAD / MIXED.
- **`claude/backtest-weather-homeaway.md`** — ⚠️ **the roof/dome PROXY and parks read null ON THE 40% LOG and are NOT confirmed on the full season** (owed test **T13a**). 🔴 **WEATHER ITSELF IS UNTESTED, NOT NULL** — temperature and wind have never been tested on any sample and no historical source exists (**T13b, BLOCKED**). ⚠️ home/road numbers superseded.
- **`claude/backtest-hits-allowed.md`** · **`claude/times-through-order.md`** · **`claude/mlb-team-advanced.md`** · **`claude/mlb-alt-line-protocol.md`**
- **`claude/today-shortlist.md`** — the current day's card.
- ⚠️ **`claude/data-starts-h.csv.md`** · **`claude/data-schedule-join.csv.md`** — 🔴 **SUPERSEDED. They are the 40%-complete log.** ⛔ **Never rebuild anything from them.**

### 🔴 🆕 TWELVE PROJECT DOCS WERE MISSING FROM THE LIST ABOVE — ADDED 2026-09-07 BY THE MONDAY SWEEP

⛔ **PRE-PUBLISH CHECK 10 HAD BEEN FAILING, AND THE v5.0 ENTRY BELOW CLAIMED IT PASSED.** `[verified 2026-09-07 against `project_info` in this same turn]` **The project holds 33 docs; the list above named 21.** 🔴 **AND THE 2026-08-31 CLAIM THAT *"the doc list above was re-verified against `project_info` in the same turn and matches"* WAS FALSE WHEN IT WAS WRITTEN — `claude/gizmos-picks-spec.md` had existed since 2026-08-22 and was never on the list, and so had `claude/mlb-data-stack.md`'s siblings for the second workstream.** ⚠️ **That is check 25 exactly: a doc asserting a verification is not a verification.** ➡️ **The twelve are added below rather than the list being rebuilt, so nothing above is disturbed.**

🔴 **AND THE MOST SERIOUS OMISSION FIRST: `claude/gizmos-picks-spec.md` IS NAMED AS REQUIRED READING IN SAM'S OWN PROJECT INSTRUCTIONS BOX AND WAS NOT IN THIS FILE'S DOC LIST AT ALL.**

**The Gizmo's Picks / product workstream:**
- 🔴 **`claude/gizmos-picks-spec.md`** — **the product spec.** ⭐ **Read before any work on Gizmo's Picks. Sam's instructions box says so; this list did not.**
- **`claude/what-every-file-does.md`** — the repo inventory: what every file in `github.com/smh0602/gizmos-picks` is for.
- **`claude/update-schedule.md`** — Sam's deadline times and the mechanism that meets them. **Owns the schedule; a prompt must point here rather than restate it.**
- **`claude/freshness-architecture.md`** — the freshness contract and the converge pass.
- **`claude/self-running-audit.md`** — whether the tabs actually run themselves, audited.

**The football / multi-league workstream:**
- **`claude/multi-league-spec.md`** — the CFB / NFL / NBA build spec.
- **`claude/nfl-checklist.md`** · **`claude/cfb-checklist.md`** — per-league state.
- **`claude/football-todo.md`** — Sam's own list.
- **`claude/football-page-checklist.md`** · **`claude/football-ui-checklist.md`** — the page and the layout match against MLB.
- **`claude/tomorrow-checklist.md`** — the NFL/CFB data-layer checklist.

### 🔴 🆕 AND THREE MORE WERE MISSING — ADDED 2026-09-14 BY THE MONDAY SWEEP. CHECK 10 WAS FAILING AGAIN, ONE WEEK AFTER IT WAS FIXED.

`[verified 2026-09-14 against `project_info` in this same turn]` **The project holds 36 docs; this list named 33.** ⛔ **The 2026-09-07 sweep added twelve and left the list CORRECT ON THAT DAY. Three docs have been created since and none of them reached this page.** ➡️ **Which is the lesson, and it is the same one the task table learned three times: A LIST OF DOCS IS NOT A THING YOU FIX ONCE. It goes stale at the rate new docs are written, and this project writes them fast.**

- 🆕 **`claude/self-healing-architecture.md`** `[created 2026-09-14]` — how the collector heals itself: what a converge pass does, what the watchdog covers, and what it cannot.
- 🆕 **`claude/halftime-markets-decision.md`** `[created 2026-09-12]` — the halftime-market probe and the decision it is waiting on.
- 🆕 **`claude/football-parlays-decision.md`** `[created 2026-09-11]` — the football SGP / parlay decision and its band-floor reasoning.

⛔ **NO DOC COUNT IS MAINTAINED HERE. The 36 above is a dated measurement inside a verification sentence — read the live list from `project_info`, every run.**

### 🔴 🆕 AND TWELVE MORE WERE MISSING — ADDED 2026-09-21 BY THE MONDAY SWEEP. CHECK 10 FAILED FOR THE THIRD TIME IN THREE SWEEPS.

`[verified 2026-09-21 against `project_info` in this same turn]` **Every doc below was created or last rewritten between 2026-09-15 and 2026-09-20, and not one reached this list.** ⛔ **Same lesson, third time: the list goes stale at the rate docs are written. Whoever creates a doc adds it here in the same turn.**

**Audits and diagnostics (dated snapshots — read as history, not as current state):**
- 🆕 **`claude/audit-2026-09-20.md`** — the 2026-09-20 narrowed audit (spend and the `credits_remaining` chain, dark crons, data checks) **and the pre-registered tests T-A–T-D, which live ONLY there** (a pointer sits in `claude/owed-tests.md` DOC DEBT).
- 🆕 **`claude/audit-2026-09-19.md`** · **`claude/audit-2026-09-19-runs.md`** — the 2026-09-19 overnight repo/product audit and its appendix on the red `collect` runs.
- 🆕 **`claude/repo-inventory-2026-09-19.md`** — a dated snapshot of the repo's modules, tests and crons, read from the code.
- 🆕 **`claude/mlb-spend-audit.md`** — what MLB's Odds API credits buy, per published leg (2026-09-19).
- 🆕 **`claude/open-questions.md`** — the running list of unresolved questions, who can answer each, and diagnoses of the red runs.

**Working docs:**
- 🆕 **`claude/repo-session-handoff.md`** — the handoff for a session that CAN push to the repo: prove push first, the binding rules, rule 253 on bot-authored `cron:` edits.
- 🆕 **`claude/line-movement.md`** — Sam's line-movement model, the 2026-09-18 movement measurement, and the news-archive gap.
- 🆕 **`claude/plan-1-8-completion.md`** — the record of football signals 1–8 (Phase A) and what is left on that plan.
- 🆕 **`claude/possession-validation.md`** — the correctness test for the football time-of-possession field.

**Ledger overflow:**
- 🆕 **`claude/pick-ledger-archive.md`** — the ledger's older slate blocks and changelog, moved VERBATIM on 2026-09-19. ⛔ **Not a second ledger and not a second denominator; every running total lives in `claude/pick-ledger.md`.**
- 🆕 **`claude/pick-ledger-2.md`** — a RETIRED overflow stub (used for ~8 hours on 2026-09-19). ⛔ **Do not write there.**

⛔ **NO DOC COUNT IS WRITTEN INTO THIS FILE.** **The 33 above is a dated measurement inside a verification sentence, not a maintained tally — read the live list from `project_info`.**

## 🔴 NO INLINE MODEL NUMBERS IN THIS FILE

`[measured]` **Every doc and task prompt that copied a coefficient has drifted, without exception.** This file's own model summary drifted twice inside two days.

➡️ **The formulas, coefficients, tier cut-points and base rates live in `claude/mlb-projection-model.md` and nowhere else. Read them there, every time.** The same rule governs task prompts: **delete the number, point at the doc.**

## Scheduled tasks

🔴 ~~**VERIFIED against `list_triggers` — RE-VERIFIED 8/21 evening, in the same turn as the edit (check 25), after all four prompts were rewritten. FOUR tasks exist and the four rows below are they.**~~ 🔴 **AMENDED 2026-08-24, Monday sweep — THREE TASKS EXIST, NOT FOUR, AND THE THREE ROWS BELOW ARE THEY.** **[verified 2026-08-24 against `list_triggers` in this same turn]** **The fourth row is STRUCK, not deleted — the strike is the record.** 🔴 🆕 **RE-AMENDED 2026-08-31, Monday sweep — FOUR TASKS EXIST AGAIN, AND THE FOUR UNSTRUCK ROWS BELOW ARE THEY.** **[verified 2026-08-31 against `list_triggers` in this same turn]** **A FOURTH task — `Gizmo's Picks — daily automation audit`, daily 15:30Z — was created 2026-08-28 and no doc knew about it.** ⚠️ 🔴 **THIS IS THE SECOND CONSECUTIVE MONDAY THAT THIS TABLE'S BOLD, DATED, EXPLICITLY-VERIFIED COUNT WAS FALSE SEVEN DAYS LATER — 8/24 struck a FOUR that had become THREE; 8/31 strikes a THREE that has become FOUR.** ➡️ **The count is not the thing to maintain. RE-READ `list_triggers` EVERY RUN and re-date the stamp; a stamp older than the thing it stamps is a claim, not a check.** ✅ **The doc list above was re-verified against `project_info` in the same turn and matches.** ⛔ **NO DOC COUNT AND NO RULE COUNT IS WRITTEN ANYWHERE IN THIS FILE.** **Name the doc, never the tally** — that mistake has been made twice in this project, once in Sam's own instructions box and once in a task prompt, and both times it was wrong within hours.

🔴 🆕 **RE-VERIFIED 2026-09-21, Monday sweep — STILL FIVE TASKS, AND FOR THE THIRD CONSECUTIVE WEEK THE COUNT WAS RIGHT WHEN IT WAS READ.** **[read from `list_triggers` in this same turn]** ✅ **Every cron unmoved: grading `30 11 * * *`, this sweep `0 10 * * 1`, the monthly re-fit `0 8 1 * *`, the daily audit `30 15 * * *`, the career board check `0 12 * * 1-5`.** ⚠️ **The daily audit's PROMPT was rewritten 2026-09-16 (its `updated_at`); its name is unchanged since 9/14. The rewrite cleared one DOC DEBT row and introduced a new inline spend figure — filed in `claude/owed-tests.md` DOC DEBT.**

🔴 **RE-VERIFIED 2026-09-14, Monday sweep — STILL FIVE TASKS, AND FOR THE SECOND CONSECUTIVE WEEK THE COUNT WAS RIGHT WHEN IT WAS READ.** **[read from `list_triggers` in this same turn]** ✅ **Every cron matches the row that describes it: grading `30 11 * * *`, this sweep `0 10 * * 1`, the monthly re-fit `0 8 1 * *`, the Gizmo's audit `30 15 * * *`, the career board check `0 12 * * 1-5` — unmoved since 9/7.** ⚠️ 🔴 **ONE ROW'S NAME HAS CHANGED, AND IT IS CORRECTED BELOW RATHER THAN LEFT: the daily 15:30Z task is now named `Gizmo's Picks — daily audit (only what the watchdog CANNOT see)`, not `…— daily automation audit`.** ✅ **ITS CRON IS UNCHANGED, WHICH IS THE POINT OF THE STANDING RULE: the CRON is the authority and it held; the NAME is prose and it moved.** ⛔ **A name is quoted in this table only so a human can find the row in the UI. Never identify a task by it.**

🔴 **RE-VERIFIED 2026-09-07, Monday sweep — STILL FIVE TASKS, AND FOR THE FIRST TIME IN FOUR WEEKS THE COUNT WAS RIGHT WHEN IT WAS READ.** **[read from `list_triggers` in this same turn]** ⚠️ 🔴 **BUT THE FIFTH ROW'S CRON HAD MOVED AGAIN, IN THE DIRECTION THE 9/1 CORRECTION STRUCK.** **`Sports + sportsbook board check` now reads `0 12 * * 1-5` — WEEKDAYS — not the `0 12 * * 1` the 2026-09-01 17:00Z re-read recorded.** ✅ 🔴 **AND THE 9/1 READING WAS NOT WRONG WHEN IT WAS MADE: at 17:00Z its `next_run_at` was Monday 2026-09-07T12:03Z, which only a Mondays-only cron produces. THE CRON CHANGED AFTERWARDS — its `last_run` fired FRIDAY 2026-09-04T12:28Z, which a `* * 1` cron cannot do.** ➡️ ⚠️ **SO THE 9/1 LESSON — "READ THE CRON, NEVER THE NAME" — SURVIVES AND IS SHARPENED RATHER THAN REVERSED: the cron is still the authority, and this time the NAME ("weekday mornings") happens to agree with it again. ⛔ THE POINT IS NOT WHICH SOURCE WINS. IT IS THAT A STAMPED ROW IS A CLAIM ABOUT A MOMENT, AND SIX DAYS IS LONG ENOUGH FOR A CRON TO MOVE TWICE.** ✅ **The row below is corrected and re-dated. The 9/1 correction is left in place as the record.**

🔴 🆕 ~~**RE-AMENDED 2026-09-01, monthly re-fit firing — FIVE TASKS EXIST, NOT FOUR.**~~ **[verified 2026-09-01 08:2xZ, AND RE-VERIFIED 2026-09-01 17:00Z on a SECOND firing of the same task, against `list_triggers` in that same turn — STILL FIVE, and this is the first re-read in three weeks that found the count unchanged]** ⚠️ **The 17:00Z re-read did find the ROW wrong, though not the COUNT: the fifth task's schedule is MONDAYS, not weekdays — see the corrected row.** ➡️ **Which is the lesson sharpened: re-reading `list_triggers` is not just counting the rows, it is reading each row's CRON.** **A FIFTH task — `Sports + sportsbook board check (weekday mornings)`, ~~weekdays~~ **MONDAYS** 12:00Z (`0 12 * * 1`) — was created 2026-09-01 01:53Z.** ⚠️ **It belongs to Sam's CAREER project (`sams career path` — a job-board check), spends no credits, pulls no odds, and touches none of this project's docs; it is recorded here only because this table's stamp claims to enumerate `list_triggers`, and without this row the stamp would be false for a THIRD consecutive time (8/24 struck a four-that-became-three; 8/31 struck a three-that-became-four; 9/1 re-dates a four-that-became-five hours after it was stamped).** ➡️ **The lesson is unchanged: RE-READ `list_triggers` EVERY RUN and re-date the stamp.**

| When | Task | Credits |
|---|---|---|
| Daily 7:30am ET | Grade yesterday's picks · **VERIFY** the opponent table. **Nothing else.** | 0 |
| **Mondays 6:00am ET** | **VERIFY the pitcher database + doc consistency sweep.** ⚠️ It cannot rebuild either database — that needs Chrome. | 0 |
| 1st of month, 4am ET | **The owed tests first** (`claude/owed-tests.md`), then re-fit, then re-run the nulls | 0 |
| 🆕 **Daily 15:30Z** | ~~**`Gizmo's Picks — daily automation audit`**~~ 🔴 **RENAMED — READ FROM `list_triggers` 2026-09-14: `Gizmo's Picks — daily audit (only what the watchdog CANNOT see)`. Cron `30 15 * * *`, UNCHANGED.** — clones the repo, counts converge passes, runs `verify_freshness.py`, checks for a blocked card, sums the day's `credits_used`, and reports whether every tab met its deadline. **Created 2026-08-28; FOUND BY THE 2026-08-31 SWEEP, which is the first time any doc recorded it.** ⛔ **Read-only: it cannot push.** | 0 |
| 🆕 ~~**Weekdays 12:00Z**~~ ~~🔴 **MONDAYS 12:00Z**~~ 🔴 🆕 **WEEKDAYS 12:00Z AGAIN — `0 12 * * 1-5`, read from `list_triggers` 2026-09-07** | **`Sports + sportsbook board check`** — job boards for Sam's career project (`sams career path`). ⚠️ **NOT part of this betting workstream; listed so this table matches `list_triggers`. Created 2026-09-01.** ~~🔴 **CORRECTED 2026-09-01 17:00Z: its cron is `0 12 * * 1` — MONDAYS ONLY, and `next_run_at` is Monday 2026-09-07T12:03Z.**~~ 🔴 🆕 **RE-CORRECTED 2026-09-07: the cron is `0 12 * * 1-5` — WEEKDAYS. It CHANGED after 9/1 and the 9/1 reading was correct at the time** (a `* * 1` cron produces exactly the Monday 9/7 `next_run_at` it recorded, and cannot produce the FRIDAY 2026-09-04T12:28Z firing `last_run` now shows). ⛔ **Neither reading was a misread. The row simply went stale in six days, which is what a stamped row does.** ⛔ **The struck "weekdays" was copied from the task's own NAME ("weekday mornings"), which disagrees with its own schedule.** ➡️ **READ THE CRON, NEVER THE NAME — a task's name is prose and goes stale like prose.** | 0 |
| ~~*(paused)*~~ 🔴 **DELETED 2026-08-23** | ~~Claude's daily MLB card — manual for now~~ ⛔ **GONE, NOT PAUSED, AND NOT COMING BACK.** **The 7:30am grading task's own prompt says so explicitly.** ⚠️ **The card is now generated UNATTENDED by `card.py` on the GitHub Actions runner — it is NOT a Claude scheduled task and must NEVER be re-added to this table as one.** | ~~—~~ |

🔴 **AND THE LESSON, RECORDED HERE BECAUSE THIS IS PRE-PUBLISH CHECK 10 FIRING ON THE DOC THAT DEFINES CHECK 10: A `VERIFIED` STAMP MEANS "VERIFIED ON THAT DATE." IT NEVER MEANS "STILL TRUE."** `[measured 2026-08-24]` **This table carried a BOLD, DATED, EXPLICITLY-VERIFIED claim — "FOUR tasks exist and the four rows below are they" — and it was FALSE THREE DAYS LATER.** ⛔ **The verification was not wrong when it was made. It was wrong when it was read, and nothing on the page said which.** ➡️ **Re-verify against `list_triggers` IN THE SAME TURN as any edit that leans on this table, and re-date the stamp. A stamp older than the thing it stamps is a claim, not a check.**

**A manual card must be logged to the ledger before first pitch, and re-logged every time it is revised.**

## 🔴 THE LINE-CAPTURE TASKS DO NOT EXIST. THEY NEVER DID.

`[measured] 8/21` **`list_triggers` returns four tasks and neither a 10:00am nor a 5:00pm capture is among them, including completed ones.**

🔴 **This doc listed two tasks that do not exist, and `claude/pick-ledger.md` spent three days explaining their silence as "no API key reachable from a scheduled run." The key was never the problem. There was nothing to run.**

➡️ **And they must not simply be created, because they could not work.** A scheduled run has **no browser**; ⛔ **`WebFetch` may never produce an odds number.** **A headless capture task would fill LINE VALUE with fabricated numbers — far worse than n=0.**

✅ **LINE VALUE is an interactive-session job, or it does not happen.**

## Who I am and what I want

Avid bettor, real money, regularly. Probability and depth, not risk warnings. Why I should AND why I shouldn't — both sides, no hedging.

Tell me when a line is mispriced or when the research contradicts a bet I like. **And tell me when there's nothing there** — a forced card is worse than an empty one.

---

# 🔴 BET CONSTRUCTION — read before ranking anything

## 🔴 CARD ONLY WHAT SAM CAN ACTUALLY BET
**PrizePicks and Hard Rock only.** Pull other books to *measure* the market and **card only those two.**
⚠️ ⏳ **OPEN QUESTION 2026-08-24, LEFT FOR SAM RATHER THAN GUESSED: ledger rule 48 was WIDENED 2026-08-23 to FIVE BOOKS (Hard Rock · DraftKings · FanDuel · Caesars · BetMGM — recorded in the repo's `CLAUDE.md`), and the repo notes the dashboard "only shows books Sam can bet at" — which reads as though he can now bet all five. Nothing records him saying so.** ⛔ **Until he answers, card only PrizePicks and Hard Rock, and pairs stay Hard Rock-only (the code enforces both).**

## 🔴 BOTH LEGS OF A PAIR MUST BE ON THE SAME BOOK
⚠️ **Hard Rock is where a pair gets built** — its prices multiply. **PrizePicks multipliers do NOT multiply**; quote probabilities and **ask Sam for the in-app number.**

## 🔴 NEVER CHOOSE THE BOOK BEFORE THE PICK
Sam, 8/21: *"your supposed to find the best bets in general that are bakced up by data you look at before you make any decisioins on a prop."*
**Order: the four mandatory inputs price the prop → the raw hit rate cross-checks it → THEN shop that rung across both books.** ⛔ **A card built toward a book surfaces props that suit a price structure instead of props the data supports.**

## 🔴 THE PAYOUT BAND — 1.8x to 2.1x

| | |
|---|---|
| **< 1.8x** | ⛔ **Not a bet. Do not show it.** |
| **1.8x – 2.1x** | ✅ **The normal card.** |
| **> 2.1x** | ⚠️ **The legs are riskier than they need to be — walk the rungs into the band.** |

🔴 🆕 **AMENDED 2026-08-26 BY SAM, AND THIS TABLE IS THE TWO-LEG ROW ONLY — FOUND IN THE CODE BY THE 2026-08-31 SWEEP, WHERE IT HAD BEEN LIVE FOR FIVE DAYS WITH NO PROJECT DOC CARRYING IT.** `[measured 2026-08-31 from `card.py`'s `PARLAY_BANDS` and the repo's own `parlay_rule` string]` **Sam, 2026-08-26: two-mans in 1.8x–2.2x, three- and four-mans in 3x–6x.** ➡️ **So the TWO-LEG target ceiling is 2.2x, not 2.1x, and THREE- AND FOUR-LEG TICKETS EXIST AND HAVE THEIR OWN BAND (3.00–6.00).** ⛔ **THE 1.80 HARD FLOOR IS UNCHANGED AND IS STILL NEVER CROSSED.** 🔴 **READ THE CURRENT BANDS FROM `claude/pick-ledger.md` RULE 28, WHICH OWNS THEM — never from this table, which describes two legs only.** ⚠️ 🔴 **AND THE CODE CONTRADICTS ITSELF HERE, REPORTED NOT FIXED SINCE 8/26: `card.py`'s `parlay_rule` prose says 1.8x–2.2x while its `in_band` flag is computed against 2.1x, so a pair at 2.15x is labelled ABOVE BAND by the flag and INSIDE BAND by the same file's prose.** **Every grading run since 8/26 has re-reported it. AN INTERACTIVE SESSION MUST PICK ONE CEILING.**

**Ladder direction — an OVER gets safer going DOWN, an UNDER gets safer going UP.** ⛔ "Drop it to 4.5" on an under is the *riskier* direction and pays *more*. **Say so when it comes up.**

**Band arithmetic, before building:** two legs at ≤2.10 must average **≤1.449 decimal (≈ −222)**. With the −700 floor (1.143) as the anchor, the partner may run to **1.837 (≈ −119)**. 🔴 **Any single leg at +110 or longer is 2.10x by itself and can never sit in the band.** Two near-even legs land at ~3.6x. **The band REQUIRES a short favourite** — and short favourites are usually priced above fair, so **the band buys hit rate with a little EV.** State the trade once; Sam chose it deliberately.

🔴 **Deliver ≥5 pairs out of the ten props**, mixing rung types, **each in the band, one book, two different games.** 🔴 **If the band cannot be filled with plays you believe in, hand over fewer and say why.**

~~**Check every pair against Sam's OPEN slips.**~~ **RETIRED 8/21 at Sam's request.** **Reusing a strong leg is deliberate. Price legs on their merits and stop narrating the overlap.**

## 🔴 AT A FLAT PAYOUT, TAKE THE LOWEST RUNG THAT CLEARS

🔴 **THE RULE STANDS. ITS JUSTIFICATION WAS FALSE AND HAS BEEN REPLACED — 8/21.** ~~`[measured]` **PrizePicks charges −137 on every rung**, so the payout never compensates for a harder number.~~ ⛔ **STRUCK: `−137` is not a price. It is the Odds API's ENCODING for a goblin rung** — see the two standing rules below.

✅ **THE REAL REASON, and it is enough on its own: PrizePicks is a DFS product. The SLIP is priced by pick-count and multiplier, and a goblin only REDUCES the multiplier slightly.** `[measured, Sam's own slips]` **real 2-pick returns of 1.8x, 1.9x and 2.0x** across goblin+goblin and goblin+standard builds. **The multiplier barely moves with rung difficulty, so taking a harder rung buys almost nothing.** Messick at rungs that paid near-identically: o3.5 **75.1%** · o4.5 66.6% · o5.5 **51.5%** — **23.6 points of hit rate given away for nothing.**

⚠️ **This is why the card's lines look low, and it is correct.** Sam, 8/21: *"most of those props you did were really low lines."* **He is right that an 8/10 on o2.5s is weak EVIDENCE.** ➡️ **Rule 35 fixes the evidence problem by shadow-grading every rung; it does not change which rung to bet.**

## 🔴 ALT LADDERS ARE MANDATORY
**Pull `pitcher_strikeouts`, `pitcher_strikeouts_alternate`, `pitcher_outs`, `pitcher_outs_alternate` every time.** Separate keys, extra credits, **budget for them.**

### 🆕 🔴 WHAT RULE 48 DOES TO THIS MANDATE — WRITTEN DOWN 8/21 BECAUSE NOBODY HAD

**Two facts now collide, and the consequence had never been stated anywhere:** **Hard Rock posts NO ALTERNATE OUTS MARKET AT ALL** (Sam-confirmed at the book, not a feed artifact) and **`us_dfs` is banned** (check 33 / ledger rule 48) — **and PrizePicks was the only book in the pull that carried alt outs.**

➡️ 🔴 **THEREFORE `pitcher_outs_alternate` UNDER ~~`regions=us,us2`~~ `regions=us2` MAY LEGITIMATELY RETURN NOTHING.** ⚠️ **MORE SO AFTER 2026-08-22, not less: the pull is now `us2` alone (check 33), so the only book that could carry an alt-outs rung is the one Sam-confirmed not to have one.** 🆕 **AND RE-QUALIFIED 2026-08-24: the pull is `us,us2` again under the five-book widening (ledger rule 48), so DraftKings / FanDuel / BetMGM / Caesars are back in the response — but the collector currently does not request `pitcher_outs_alternate` at all (ledger rule 61, decided when Hard Rock was the only book). Whether the five-book pull should re-request it is an INTERACTIVE decision for Sam, not one a sweep makes.**

- ✅ **THE PULL STAYS MANDATORY.** ⛔ **Do not skip a market because you expect it to be empty.** **That is how ~~"Hard Rock posts no alt ladders"~~ got written down twice** — an expectation standing in for a query.
- ✅ **AN EMPTY RESULT IS A FINDING AND IS REPORTED, not silently absorbed.** **State the market key, the regions requested and the row count** — *"`pitcher_outs_alternate` returned 0 rows across `us2` on N games."*
- 🔴 **SAY WHAT IT PROVES AND WHAT IT DOES NOT.** **"The feed returned no alt-outs rows under `us2`"** — ⛔ **never "there are no alternate outs."** **An absence in a query is not an absence in the world, and that is the failure this project has recorded five times.**
- ⛔ **A DFS RUNG MAY NOT BE SUBSTITUTED.** **A PrizePicks alt-outs rung has no price, sits in a banned region, and must never fill the gap.** 🔴 **Consequence, stated plainly: an outs ladder cannot be cross-checked rung-for-rung anywhere** — so an alt-outs play simply may not exist on a given card, and **that is the correct outcome, not a coverage failure to paper over.**
- ⚠️ **And an "outs ladder" attributed to Hard Rock is a MISLABELLED STRIKEOUT LADDER** — `'pitcher_strikeouts'.includes('outs')` is TRUE (check 23). **Match the key exactly before believing the rows.**

🔴 **MEASURED 8/20, CORRECTED BY SAM 8/21 — what each book actually posts:**

| | Hard Rock | PrizePicks |
|---|---|---|
| K alternate | **9–11 rungs, real multiplying odds.** ⚠️ **The FEED returns only the Over side; the BOOK offers both** | 5–7 rungs, overs only in the feed |
| **Outs alternate** | 🔴 **NONE AT ALL — Sam-confirmed at the book, not a feed artifact** | ✅ 2–4 rungs |
| Outs standard | ✅ one two-way line | sometimes |
| Pricing | real multiplying odds | 🔴 **NO PER-LEG PRICE EXISTS.** The feed's `−137` / `+100` are **LABELS** — goblin / demon — not quotes. **The slip is priced by pick-count and multiplier. ASK SAM.** |

🔴 **THE FEED IS NOT THE BOOK.** ➡️ **Procedure: when the band math wants an alternate UNDER on Hard Rock, name the rung, say the feed does not carry its price, and ASK SAM to read it off the app.** ⚠️ **Never assert a PRICE you have not seen.**

⛔ **Do not re-open the question of whether the API carries Hard Rock's alt unders — that is closed owed test T14.**

## 🔴 SAM'S ALT-RUNG FLOOR — −700, nothing shorter
⛔ **NEVER name a rung without having seen it in a raw pull.** Three phantom rungs have shipped. **Sam caught all three.**
⚠️ **A −700 floor is a HARD ROCK filter and can only ever be applied there.** 🔴 **PrizePicks rungs carry no price at all, so the floor cannot be evaluated on PrizePicks — do not pretend it has been.**

## Stakes
⚠️ `[measured]` **Sam's slips run $25–30.** **Ask rather than assume, and record it.** Quote EV as a percentage **and in dollars at his real stake.** 🔴 **On a PrizePicks play there is no EV to quote until Sam supplies the multiplier — see the standing rule below.**

## Rank by PROBABILITY, subject to the payout constraint
**Filter to legs that can pair inside the band, then rank by how likely they are to hit.** Show the edge alongside so market disagreement is visible, but **the price never disqualifies a play.** 🔴 **On PrizePicks there is no edge column to show. Rank by probability and stop there.**

## One leg per pitcher
Ranking raw legs by EV once put **four Alcantara rungs in a top 10.**

## 🔴 OVERS ARE FOR GOOD ARMS
**K and outs OVERS on GOOD arms. UNDERS are where BAD arms belong.**
⚠️ **MIXED widens this deliberately — say so when you use it.** High-K/high-ERA → **K overs only**; low-K/low-ERA → **outs overs only**. **Flag it so Sam can veto.**
⛔ **Don't fade good pitchers on strikeout unders.** **Fade workload instead — an outs under is a different bet.**

## Coverage — no silent narrowing
**Every named starter. All four markets.** ~~**Both books.**~~ ⚠️ 🔴 **QUALIFIED 8/21 evening — "both books" NO LONGER MEANS BOTH BOOKS ARE PULLED.**

✅ **PrizePicks is still a book Sam BETS**, and a PrizePicks play may still be carded on `model / raw / blend / matched class` with the multiplier **ASKED FOR** (ledger rule 46). ⛔ **But it is NO LONGER PULLED FOR A NUMBER** — check 33 / ledger rule 48.

➡️ ~~**Coverage now means: every named starter and all four markets across `regions=us,us2`.**~~ 🔴 **NARROWED 2026-08-22 (check 33): coverage means every named starter and all four markets across `regions=us2` — Hard Rock only.** 🔴 🆕 **WIDENED 2026-08-23 — SEE LEDGER RULE 48, WHICH OWNS THE CURRENT REGION/BOOK SET: FIVE BOOKS (Hard Rock · DraftKings · FanDuel · Caesars · BetMGM) and the props pull is BACK ON `regions=us,us2`.** **Sam's instruction, recorded in the repo's `CLAUDE.md` / commit `91b0290` and found by the 8/24 Monday sweep.** ⛔ **`us_dfs` stays banned.** ➡️ **Read the current set from `claude/pick-ledger.md` rule 48 — never from this line, which has now gone stale twice in three days.** **PrizePicks appears on a card as a place to bet, never as a Price column.** Anything dropped is **named with the reason.**
**Cross-check the odds event list against the statsapi schedule.** 🔴 **A missing game is not a missing game.** **Name every gap and ask Sam to check.**

## 🔴 RESOLVE EVERY STARTER'S TEAM FROM statsapi, TWICE
`[measured] 8/20` The odds feed groups both starters in one event with **no team field.** One flip inverts **two** mandatory inputs at once and moved one pitcher's `E[K]` by **+0.79**.
⛔ **Resolve from `schedule?hydrate=probablePitcher`, then confirm against his own game log** — a pitcher never faces his own team.

## 🔴 A PRIZEPICKS PLAY MAY BE RANKED BY PROBABILITY BUT MUST NEVER CARRY A BREAK-EVEN, AN EDGE, OR AN EV FIGURE

🔴 **`−137` IS NOT A PRICE. IT IS THE ODDS API'S ENCODING FOR "GOBLIN" (the easier rung); `+100` IS ITS ENCODING FOR "DEMON" (the harder rung).**

`[measured 2026-08-21]` Across **7 games × 4 markets**, PrizePicks returned **exactly two distinct values in 145 outcomes: −137 (×77) and +100 (×68).** Same pull, same call: **hardrockbet 80 distinct prices · espnbet 73 · betparx 28 · underdog 27 · fliff 23 · ballybet 18 · pick6 just 1.** **No sportsbook prices 145 props at two numbers. It is a label, not a quote.**

⛔ **CONSEQUENCE, stated bluntly: every "break-even 57.8%", every "edge +X" and every EV figure ever computed on a PrizePicks play is arithmetic performed on a constant. Those numbers are meaningless — not merely imprecise.**

⚠️ **PrizePicks does not price rungs at all.** It is a DFS product: **the SLIP is priced by pick-count and multiplier, and a goblin reduces the multiplier.** **There is no per-leg American price to break even against.**

➡️ ✅ **PROCEDURE: quote `model / raw / blend / matched class`, then ASK SAM FOR THE IN-APP MULTIPLIER.** ⛔ **Do not derive one from the feed.**

## 🔴 BEFORE TREATING ANY BOOK'S PRICES AS REAL, COUNT ITS DISTINCT VALUES IN THE PULL

🔴 **A book returning 1–2 distinct prices across dozens of outcomes is emitting LABELS, NOT QUOTES.** It is a one-line check — group the outcomes by bookmaker, count distinct `price` values — **and it would have caught this error on day one.**

⚠️ **FIFTH INSTANCE of this project's most repeated failure: a fact about a QUERY written down as a fact about the WORLD.** The others: ~~"Hard Rock posts no alt ladders"~~ · ~~"Hard Rock's alt ladders are overs-only"~~ · WebFetch's 27 fabricated events · the feed omitting Luzardo's entire market while carrying the game. 🔴 **This one is the worst because it was load-bearing for the project's stated premise, and it survived every audit for days.**

⚠️ **ALSO OPEN — DO NOT RESOLVE BY GUESSING.** Sam says Burke's 3.5 rung *"is not priced at −137."* **That confirms the price is fake; it does NOT establish whether the 3.5 rung exists in the app at all.** The feed has previously shown rungs that did not exist (three phantom rungs) and hidden rungs that did. ⛔ **Recorded as an open question for Sam. Do not assert the rung either way.**

## Delivery format
Per play, in order: **1) the player · 2) the odds** (book, price, payout) · **3) the data and why** · **4) the matchup** (opponent, first pitch, moneyline, conditions). Then the pairs, ranked by joint probability. **One block per play.** 🔴 **On a PrizePicks play, field 2 carries the rung and the book and NOTHING ELSE until Sam supplies the multiplier.**

🆕 🔴 **AND THE CARD TABLE MUST CARRY THREE PROBABILITY COLUMNS, NOT ONE: `model`, `raw hit rate at that threshold`, and `blend`.** `[measured 8/21]` **Every card to date logged the blend alone, which means rule 15's scoreboard has never once been executable from the record.** The 8/20 numbers had to be reconstructed from scratch — **it worked, but a reconstruction is not a log.**

---

# THE GOAL, PRECISELY

**Predict outcomes, then use those predictions to find a good price.**

### The model doesn't have to be good. It has to beat the specific number it's attacking.
Against a **sharp sportsbook line**, ~15% R² is never enough. 🔴 **Against a DFS payout structure it can still win — but the reason is NOT what this doc said for three days.**

~~**Hard Rock charges −750 for Nola over 3.5 K; PrizePicks charges −137 for the identical bet.**~~ ⛔ **STRUCK 8/21 — FALSE, AND IT WAS THE PROJECT'S STATED PREMISE.** **The −137 was never a PrizePicks price.** It is the feed's encoding for "goblin." **There was never a −750-vs-−137 comparison to make: one side of it is a real quote and the other is a label.**

✅ **WHAT SURVIVES, PRECISELY — do not over-correct:**
- ✅ **The model probabilities are UNTOUCHED.** Nothing about `E[K]`, `mu`, the raw rates or the matched-class check depends on this.
- ✅ **Hard Rock prices are REAL** — 80 distinct values in the same pull. **Every Hard Rock edge, break-even, de-vig and pair multiplier STANDS.**
- ✅ **"At a flat payout, take the lowest rung that clears" SURVIVES with a new justification:** not that PrizePicks charges −137 everywhere, but that **the 2-pick multiplier barely moves with rung difficulty** — Sam's real slips returned **1.8x, 1.9x and 2.0x.** **A harder rung for a near-identical multiplier is still a donation.**
- ✅ **The DFS thesis survives in WEAKENED form:** PrizePicks' payout structure is insensitive to rung difficulty in a way a sportsbook's is not. ⚠️ **It can no longer be quantified from the feed, and it was never measured — it was inferred from an encoding.**

**Hunt lazy mechanical payout structures, not general edge — and prove the laziness from a real payout, never from an encoding.**

### Two things get measured, in order
**1. Calibration.** When it says 65%, does it hit 65% over 50 picks?
**2. Line value.** Gap to the **sharp market**, never the sticky DFS line. **No conclusions under ~50 paired observations.**

**The failure mode:** mean gap strongly positive while hit rate sits at or below break-even = **a model that is confidently wrong.**

---

# 🔴 THE FOUR MANDATORY INPUTS — no exceptions

> *"you should ALWAYS be factoring those in when you are evaluating bets, no exceptions"*

**1. His own form · 2. WHO HE IS FACING · 3. HOME OR ROAD · 4. His raw hit rate at that exact threshold.**

**Every market. Every play. State all four in the writeup even when a term contributes ~zero.** **A projection missing any of the four is not finished.**

🔴 **The formulas are in `claude/mlb-projection-model.md`. Read them there.** What that doc establishes, and what no card may contradict:
- **No opponent term in the OUTS model** — measured null.
- **The previous-start variable is PITCH COUNT, not outs.**
- **Long-outing starters have almost no explainable variance.** ⚠️ **Do not expect model edge on a workhorse's outs line.**
- **Whole-number lines push.** Quote the **push-adjusted** probability, `P(win)/(P(win)+P(loss))`.
- **Earned runs and hits allowed: no fitted model, no plays.**
- **The blend** = 50% model, 50% raw season rate. ⚠️ 🆕 **The 50/50 is HAND-SET and, as of 8/21, KNOWN TO BE UNIDENTIFIABLE at current sample sizes — the Brier surface across w=0 to w=1 spans 0.0011 at n=12.** ⛔ **Do not tune it. Owed test T15.** Outs distribution **Normal(mu, SD = cvO × season mean outs)** — ⚠️ **never fitted. Owed test T3.**

### MIXED — ~36% of pitchers are neither GOOD nor BAD
**MIXED changes WHICH MARKET you bet, not the weights.** **Never lean on the tier label for a pitcher near a cut point — read the raw K/9 and ERA.**

---

## Required framework — pitcher props

⚠️ **Cite these steps by NAME, never by number.**

1. **Classify GOOD/BAD/MIXED from prior starts only.**
2. **His last 8 starts** — `mK8` / `mO8` from the pitcher database. ⚠️ **Exclude rows with `mO` under ~9 — those are openers.**
3. **Opponent meanK** from `claude/mlb-opponent-database.md`, centred on **that doc's stated constant**. 🔴 **Strikeouts only.**
4. **Run the formula from the model doc.** No freehand estimates.
5. 🔴 **CROSS-CHECK AGAINST HIS RAW HIT RATE AT THAT EXACT THRESHOLD.** **A gap over ~10 points means the model is the suspect.** 🆕 **Log both numbers, not just the blend.**
6. **Shape, not just level** — `cvO` and `tail`. 🔴 **`cv` is the SAMPLE SD (n−1).**
7. 🔴 **HIS PREVIOUS START'S PITCH COUNT** — it is the model term. ⛔ **Do not price the previous start's OUTS.** ⛔ **Do not price the reason** he was short. ⚠️ **But name an unusual count as context.**
8. **Workload context** — IL, career-high innings, converted from the pen.
9. **Moneyline.** A favourite's starter pitches with a lead. `[hypothesis]`.
10. **De-vig Hard Rock's two-way base price** as the market check. 🔴 **Hard Rock only — there is nothing on PrizePicks to de-vig.**

### Banned arguments — tested and failed

- ⛔ "Good swing-and-miss stuff, so he'll go deep." R²=0.0008.
- ⛔ "This lineup racks up hits / he's been getting hit." Both R² ≈ 0.000.
- ⛔ "He'll go 6+, so more hits." **Wrong in sign** (corr = −0.548).
- ⛔ 🔴 **"This lineup gets to starters early, so fade the outs."** **The opponent does not predict outs.**
- ⛔ **"He went short last time."** Previous-start *outs* is zero. **Use PITCH COUNT.**
- ⛔ "Extra rest, he'll go deeper." Significant but **negative and confounded.**
- ⛔ "Adjust the opponent for pitcher quality faced." Real mechanism, **t=0.0.**
- ⛔ "It's a dome, so it's more predictable." Null on level **and** variance.
- ⛔ "**This pitcher** is better at home." Per-pitcher splits run n=1–7. ✅ **The LEAGUE term is mandatory.**
- ⛔ "Coors/this park hurts him." Parks sit inside the opponent number.
- ⛔ "Same-handedness opponent log." **Dead twice.**
- ⛔ "This pitcher is MIXED, so lean on the matchup." Interaction null.
- ⛔ "They're a sub-.500 team, so it's a soft spot." **meanK already contains it.**
- ⛔ "This lineup grinds at-bats, so he gets pulled early." ✅ **The mechanism is real at every link** and **it does not reach outs.** ⚠️ **"Grinds at-bats" and "gets hits" are OPPOSITE lineup types** (corr −0.401). **Do not bundle them.**
- ⛔ 🆕 **"PrizePicks is charging −137 for a bet Hard Rock charges −750 for."** **STRUCK 8/21 — the −137 is an encoding, not a price. There is no PrizePicks price to compare.**

### 🔴 Book coverage — HARD ROCK IS IN `us2`

| Region | Books |
|---|---|
| `us` | DraftKings · FanDuel · BetMGM · BetOnline · Bovada · BetRivers |
| `us2` | **hardrockbet · hardrockbet_oh** · espnbet · fliff · betparx · ballybet |
| `us_dfs` | PrizePicks · Underdog · Betr · DK Pick6 |

~~**A full both-books card needs all three regions — 3× the credits per market.**~~ 🔴 **STRUCK 8/21 evening — IT CONTRADICTED THIS DOC'S OWN CHECK 33.** **Ledger rule 48 bans `us_dfs`, so it is not requested at all and is therefore not a third region in the cost.**

~~✅ **RESTATED: a card needs TWO REGIONS — `regions=us,us2` — and 2× the credits per market.**~~ 🔴 **NARROWED 2026-08-22 (check 33): a card needs ONE REGION — `regions=us2` — and 1× the credits per market, because Hard Rock lives in `us2` only.** 🔴 🆕 **WIDENED 2026-08-23 (ledger rule 48, five books): TWO REGIONS — `regions=us,us2` — and 2× the credits per market. Read the current set from ledger rule 48.** ⚠️ **Report the per-game figure from the pull rather than assuming it.** ⚠️ **The `us` and `us_dfs` rows stay in the table above as a record of WHAT EACH REGION CONTAINS — ⛔ neither is pulled, and neither is a source of a number.**

⚠️ `hardrockbet` and `hardrockbet_oh` are **DIFFERENT BOOKS.** ⚠️ **De-vig within a single book.**

**A gap in your query is not a gap in the data. Verify which one it is before writing it down.**

⚠️ 🔴 **`'pitcher_strikeouts'.includes('outs')` is TRUE.** **Match market keys EXACTLY.**

---

## Break-evens and conversions

⚠️ 🔴 **THIS TABLE APPLIES TO REAL PRICES ONLY — Hard Rock and the sportsbooks. It MUST NOT be applied to a PrizePicks outcome, whose `−137` / `+100` are labels.**

−650 86.7% · −500 83.3% · −300 75.0% · −250 71.4% · −200 66.7% · −155 60.8% · ~~**−137 57.8%**~~ 🔴 **STRUCK 8/21 — this entry existed only to price PrizePicks goblins and it priced a label. A genuine −137 quote from a real book still breaks even at 57.8%; the entry is struck because in this project it was never used that way.** · −110 52.4% · +100 50% · +120 45.5% · +150 40% · +200 33.3%

Parlay joint break-even: **1.8x → 55.6% · 1.9x → 52.6% · 2.0x → 50% · 2.1x → 47.6%** · 3.0x → 33.3%

Decimal: −700 **1.143** · −300 1.333 · −250 1.400 · −200 1.500 · −150 1.667 · −110 1.909 · +100 2.000 · **+110 2.100**

Outs: 17.5 → 6.0 IP (18 outs) · 16.5 → 5.2 IP (17) · 15.5 → 5.1 IP (16). `.1` = 1 out, `.2` = 2 outs.
🆕 ⛔ **`.3` is not a valid IP fraction. A fetched value that violates the domain is fabricated until proven otherwise — ledger rule 40.**

---

## Required framework — NFL

Untested — no backtest exists. Shrink hard. Defensive matchup vs QB archetype · passing/rushing yards allowed · WR1/WR2/TE splits · snaps, target share, air yards · practice participation · weather.

**Before building an NFL model, find the NFL equivalent of the mechanical ladder** — the market priced as a schedule rather than a prediction.

---

## Where this is going — 🔴 THIS BECOMES A WEBSITE

Sam, 8/19: *"after you have enough practice reps your gonna be turning this into a website. and youll operate from there so we need this all to work."*

**On a website there is no Sam reading the output.** Every error he catches personally has to become an assertion that fails loudly, not a warning in a doc a future session may or may not read.

**The practice reps are for finding failure modes, not for getting picks right.** A wrong pick costs one bet. **A wrong process that nobody catches ships every day.**

### 🔴 PRE-PUBLISH CHECKS — a card does not ship until all pass

| # | Assert | Fails if |
|---|---|---|
| 1 | Opponent meanK built from **statsapi over every start**, and centred on **the constant that table states** | the retired StatMuse build adds ≈ +0.15 K to every projection |
| 2 | Every subgroup label computed from **prior starts only** | a season-total label leaks the outcome |
| 3 | Odds event list **reconciles** against the statsapi slate; every gap named | a dropped event silently deletes plays |
| 4 | ~~All three regions requested~~ ~~🔴 **`regions=us,us2` requested, or the dropped one named.**~~ 🔴 **`regions=us2` requested — the ONLY region pulled. `us` and `us_dfs` are NEVER requested** (check 33, narrowed 2026-08-22) 🔴 🆕 **WIDENED 2026-08-23: `regions=us,us2` requested (five books — ledger rule 48); `us_dfs` still NEVER** | a book reported absent that was never requested |
| 5 | Every alt rung **re-pulled alone** with a row count | truncated ladders invent rungs |
| 6 | Model probability vs **raw hit rate at that threshold**, gaps >10 pts flagged | smoothing breaks on metronomes |
| 7 | De-vig computed **within one book** | cross-book best/best is not a market price |
| 8 | Card has **one table, one numbering, one LABEL, a stated size, a market split that sums, and W/L counts that sum** | a grader cannot identify the card |
| 9 | Every play states **all four mandatory inputs** | a term was silently skipped |
| 10 | Doc list and task table **match reality** — verified, not assumed | 8/21: the task table listed two tasks that did not exist |
| 11 | 🔴 **Every carded price is on PrizePicks or Hard Rock** | the card is unusable |
| 12 | 🔴 **Both legs of every pair carry the SAME book name** | the parlay cannot be placed |
| 13 | 🔴 **On a flat ladder, no rung is carded if a lower one clears** | paying nothing for a harder number |
| 14 | **Every play states its moneyline and first pitch** | an incomplete block |
| 15 | 🔴 **No payout multiplier asserted unless Sam read it off the app** | pricing a bet that doesn't exist |
| 16 | 🔴 **Every logged bet carries stake, multiplier, return, running P&L** | ROI unknown while calibration is argued |
| 17 | 🔴 **Every pitcher input comes from a log with COMPLETE season coverage** | attenuation bias — **cost a whole model version** |
| 18 | 🔴 **Any hand-written statistical routine verified against a reference implementation** | an unverified regression is not evidence |
| 19 | 🔴 **No odds number reaches a card through `WebFetch`** | it fabricates — 27 invented events, 3 of 9 wrong prices |
| 20 | 🔴 **Every carded rung appears in a raw pull, with its book and price** | phantom rungs. Three shipped |
| 21 | 🔴 **Every pair's multiplier is the PRODUCT OF TWO DECIMALS, printed, and lands in** ~~**1.8x–2.1x**~~ **its ticket's band — `claude/pick-ledger.md` rule 28 owns the bands (1.80 hard floor on every ticket; corrected 2026-09-21 — this check had carried the pre-2026-08-26 ceiling for four weeks)** | 8/20 shipped five pairs at 2.42x–2.94x |
| 22 | 🔴 **Every starter's TEAM resolved from `hydrate=probablePitcher` AND confirmed against his game log** | one flip inverts opponent *and* home/road |
| 23 | 🔴 **Market keys matched EXACTLY, never by substring** | `'pitcher_strikeouts'.includes('outs')` is true |
| 24 | ~~Every pair checked against Sam's open slips~~ — **RETIRED 8/21 at Sam's request** | — |
| 25 | 🔴 **No doc or prompt claims a fix without it being verified in the same turn** | 8/20: a doc asserted a task was rewritten. It was not |
| 26 | 🆕 🔴 **The card logs `model`, `raw` and `blend` as SEPARATE columns** | 8/21: rule 15's scoreboard had never once been executable from the record |
| 27 | 🆕 🔴 **Every fetched value is domain-checked against what its field can physically hold** | 8/21: a `1.3 IP` sailed through the "safe" headless path and was caught only by a season-sum control |
| 28 | 🆕 🔴 **Every count stated in prose reconciles against the ROWS beneath it, and any row not part of the tally says so inline** | 🔴 **WRONG COUNTS HAVE SHIPPED REPEATEDLY — the worst kind sum correctly and pass their own checkmark** (a market split logged "7 K + 2 outs = 9 ✅" when it was 8+1). ⛔ 🆕 **DO NOT RE-ENUMERATE THE INSTANCES HERE. The single canonical list is `claude/pick-ledger.md` ledger rule 45** *(corrected 8/21 evening — this check's own count had failed this check; the duplicate enumeration was removed 8/21 late, after three disagreeing copies of the list were found, which is this check's defect in its own text).* ⚠️ **One instance lives OUTSIDE the ledger and is therefore named here, since rule 45 is scoped to that doc: a doc claiming "four superseded meanK values" over five struck rows.** |
| 29 | 🆕 🔴 **Results tables are APPEND-ONLY. "Prune" never applies to a graded row, a money row or a dated card table** | **a five-play graded table was silently deleted** by a full-file rewrite carried out under a standing instruction to prune |
| 30 | 🆕 🔴 **Any doc containing graded results is edited by READ → local file → programmatic patch → upload with `local_path`, and the graded-row count is reported before and after** | **a rewrite from context is a retype, and anything not retyped is deleted with no error, no warning and no diff** |
| **31** | 🆕 🔴 **NO PrizePicks play carries a PER-LEG break-even, edge or EV figure — banned ALWAYS. Probability columns only, then ASK SAM for the in-app multiplier.** ✅ 🆕 **SLIP-LEVEL EV IS PERMITTED once he reports it** — `EV = stake × (joint × multiplier − 1)`, break-even `1 / multiplier` — **because that multiplier is a real quoted payout** *(amended 8/21 late; ledger rule 46 and `claude/card-blueprint.md` STEP 6 item 4 say the same thing)* | 8/21: five of ~~seven~~ **EIGHT** carded plays shipped quoted PER-LEG edges of **+27.6, +19.1, +18.2, +18.1, +12.4** — every one of them arithmetic performed on a constant. *(Denominator corrected 8/21 late against `claude/pick-ledger.md`: the card is EIGHT plays, five of them PrizePicks rows.)* |
| **32** | 🆕 🔴 **For every book whose prices are used as prices, the DISTINCT PRICE COUNT in that pull is stated. 1–2 distinct values across dozens of outcomes = LABELS, not quotes** | 8/21: PrizePicks returned **2 distinct values in 145 outcomes** while hardrockbet returned **80** — a one-line check that would have caught the project's central error on day one |
| **33** | 🆕 🔴 ~~**ODDS ARE PULLED WITH `regions=us,us2` AND NEVER `us_dfs`.**~~ 🔴 **NARROWED 2026-08-22 — ODDS ARE PULLED WITH `regions=us2` AND NOTHING ELSE. HARD ROCK ONLY, EVERY PROP, EVERY CARD.** **`us_dfs` was already banned and stays banned; `us` is now dropped too.** **Hard Rock lives in `us2` only, so one region is correct and sufficient.** **No PrizePicks value appears in a Price column, ever** 🔴 🆕 **WIDENED 2026-08-23 — SEE LEDGER RULE 48, WHICH OWNS THE CURRENT REGION/BOOK SET: FIVE BOOKS (Hard Rock · DraftKings · FanDuel · Caesars · BetMGM) and the props pull is BACK ON `regions=us,us2`.** **Sam's instruction, recorded in the repo's `CLAUDE.md` / commit `91b0290` and found by the 8/24 Monday sweep.** ⛔ **`us_dfs` stays banned.** ➡️ **Read the current set from `claude/pick-ledger.md` rule 48 — never from this line, which has now gone stale twice in three days.** | Sam, 2026-08-22: *"i onlt want to see hardrock odds from now on for all props"* — superseding Sam, 8/21: *"from now on i want you to pull all odds from sportsbooks not prizepicks… let's just pull our odds from hardrock and other sports books."* **Enforced at the REQUEST, not at the write-up — a price that never enters the working set cannot be quoted.** 💰 **And it halves the bill: `cost = unique markets × regions, per game`, so 4 × 1 = 4 credits per game against the old 4 × 2 = 8** `[measured 2026-08-22, `x-requests-last: 1` on a one-market one-region probe]` |
| **34** | 🆕 🔴 **A Hard Rock alt strikeout rung is quoted in BOTH forms — the feed's `oN.5` and the app's `N+1 strikeouts` — and every price is stamped with the minute it was pulled** | the app labels the feed's **Over 3.5** as **"To Record 4+ Strikeouts"**; reading it off by one turns **−1000 into −400** and moves the break-even **10.9 points.** And Misiorowski's o6.5 moved **−550 → −850 in 32 minutes** |
| **35** | 🆕 🔴 **When the pitcher's profile and the lineup's profile CONTRADICT at the carded number, the DIRECT HEAD-TO-HEAD is run — THIS SEASON AND LAST SEASON ONLY — and its `n` is PRINTED ON THE ROW, including `n=0`** | **ledger rule 50 had no enforcement anywhere in this doc.** ⚠️ **It is a TIEBREAKER, never an input, and is never folded into the blend** — on most nights it is a sample of one or two starts. `[measured 8/21]` **four of the TEN ARMS CHECKED had NEVER faced that night's opponent in 2025–26 — Burke, Prielipp, Manaea, Chandler; "none" is a finding and must be printed, not omitted.** ⚠️ 🆕 ~~"four of ten CARDED arms"~~ **corrected 8/21 late against ledger rule 50 — TABLE A is eight plays and contains no Prielipp (rule 45).** 🔴 **And it is reported when it argues AGAINST the card — on its first run it contradicted Claude twice.** **A tiebreaker only quoted when it agrees is not a tiebreaker** |
| **36** | 🆕 🔴 **A carded play against a lineup the pitcher faced INSIDE 30 DAYS states the GAP IN DAYS and applies the threshold haircut — and any citation of that effect says the pre-registered specification FAILED and the threshold version is EXPLORATORY** | **ledger rule 51 had no enforcement anywhere in this doc.** **A rematch is a reason to DROP A RUNG, not to drop the pitcher** — roughly **8 points at a 4+ K bar** and **3 points at a 3+ K bar.** 🔴 **The registered mean-K specification FAILED at t=−1.78; the significant version is a second cut of the same data.** **Quoting it without that sentence attached is specification shopping with the label removed** — owed test **T18** |
| **37** | 🆕 🔴 **A TABLE C row is created ONLY from Sam's own report — a screenshot, or his words naming the slip he placed. A proposed slip lives in PROSE, never in the bet table** | 8/21: **that table was WRONG TWICE before his screenshots arrived.** Rodríguez + Manaea and Rodríguez + Burke were written in as placed bets when they were **Claude's recommendations**; the real slips were **Manaea + Rodríguez** and **Dobbins + Melton**, and **Burke was never bet at all.** ⚠️ **Nothing was graded off the wrong rows and no money figure moved — but this is exactly how a fabricated bankroll row gets written** (ledger rule 24) |

🔴 **Any check that only exists as prose in a doc is not a check.**

### The other build targets

Betting and fantasy tools — mispriced lines across baseball and football, waiver-wire and workload projection. Sam is targeting sports management or sports media, plus fantasy football content for TikTok and Instagram. **The site is the portfolio piece.**

⚠️ **Before it goes public: the Odds API key was pasted in a chat transcript and should be rotated.** Sam has declined for now; **it is a pre-launch blocker, not a today problem.**

**The backtest method:** freeze the clock the day before, compute the predictor from prior data only, record prediction and outcome, repeat ~1,000 times, check whether the buckets separate. 🔴 **AND THE GROUPING VARIABLE COUNTS** — a label built from season totals is lookahead. **Distinguish mechanism from forecast** — repeatedly, a real causal mechanism has turned out to be a null predictor. **That is the default expectation, not a surprise.**

---

## Working agreement

**Direct write access to these docs, used every time, without being asked.** Edit, then report what changed.

🔴 **When Sam proposes a mechanism, TEST IT — don't accept it and don't dismiss it.** `[measured]` He is repeatedly **right about the spot and wrong about the reason.** ✅ **On pitch count he was right about BOTH, and the project tested the wrong proxy for a week.** **His pattern recognition consistently outruns his explanation of it — and sometimes it outruns ours.**

⚠️ **And sometimes his instruction is directionally wrong on half the board.** **Implement the intent, name the exception, never silently do the wrong thing.**

🔴 **Fix problems the moment they are found.** *"if you ever notice anything is wrong you fix it IMMEDIATELY."*
🔴 **Log after every correction and every new finding — not at the end of a session.**
🔴 **Updating one doc is not the job. Sweeping the ones that now disagree is.**
🔴 **If a number gets recomputed on every card, it belongs in a doc.**
🔴 **Check the DATA before doubting the MODEL.**
🔴 **A doc asserting a fix is not a fix.** Verify at the source in the same turn.
🆕 🔴 **AND A BYTE-DIFF PROVES THE WRITE MATCHED WHAT YOU BUILT — NOT THAT WHAT YOU BUILT WAS RIGHT.** `[measured 2026-08-24]` **The two halves of ledger rule 43's verification answer DIFFERENT questions and neither substitutes for the other: the post-write byte comparison catches a corrupted TRANSMISSION, and the `collections.Counter` line-loss check catches a wrong PATCH.** ⚠️ **A document-wide replace of `THREE TIMES` in a task prompt would have produced a correct-looking edit at the anchor and a nonsense `FOUR TIMES IN THREE DAYS` in a bullet added minutes earlier — and the byte-diff would have PASSED, because the write faithfully transmitted the wrong content.** ✅ **The uniqueness assertion caught it, and the part that generalises is that it fired UNCONDITIONALLY rather than because anyone suspected a collision** — ⛔ **the check has to run even when you are confident, because the case where confidence is misplaced looks identical from the inside.** ➡️ **Scope every anchor to the smallest region that makes it unique, and read the Counter result as the correctness check, never the byte-diff.**
🆕 🔴 **AND VERIFY BY CONSISTENCY, NOT BY EQUALITY, WHEN THE PUBLISHED VALUE IS ROUNDED.** `[measured 8/21]` Recomputing the opponent table's Δ column from its published 2-dp meanK flags **five correct rows** as errors. **A verification run that "fixed" them would have introduced error and logged it as a fix — check 25 in reverse.**
🆕 🔴 **AND BEFORE COMPUTING WITH A FEED VALUE, ASK WHAT IT IS.** `[measured 8/21]` **A field named `price` held an ENUM.** ⛔ **A number's column heading is not a warrant that it is that kind of number. Count its distinct values — check 32.**
⚠️ **`project_write` is delete-then-create. A 503 mid-write DESTROYS the doc.** **Draft locally, upload with `local_path`.**

🔴 **`project_write` REPLACES THE WHOLE DOCUMENT AND SHOWS YOU NOTHING.**
There is no patch, no merge, no diff. **Every "edit" is a full retype from whatever is in context, and whatever is not retyped is gone** — no error, no warning, and nothing left behind except a reference that no longer resolves.
`[measured]` **This deleted TABLE B — the 8/19 midday card, five graded plays — and it went unnoticed for two days.** It was combined with this project's own standing instruction to **prune**, which is correct for prose and catastrophic for records.
✅ **THE METHOD, not optional on any doc that holds a record:**
**1.** `project_read` the CURRENT version — never write from a copy loaded earlier in the session.
**2.** Save it verbatim to a local file.
**3.** Patch it programmatically, asserting each anchor matches exactly once.
**4.** Upload with `local_path`.
**5.** **Count graded rows before and after. Report both numbers.**
⚠️ **A concurrent write makes step 1 mandatory even minutes later** — a scheduled task and an interactive session have already collided on the same file in the same day.

🔴 🆕 **HOW A REPO FIX IS DELIVERED — THE PROTOCOL, ADDED 2026-09-20 AFTER A DELIVERY SILENTLY REVERTED A MERGED PR.**
`[measured]` **Sam's `Downloads` holds ~150 `gizmos-*` folders from this project, many containing a file called `card_fb.py`, `nfl.py` or `collect.py`. File Explorer shows no way to tell them apart.** ⛔ **A file uploaded from the wrong one arrives as a well-formed commit with the right message and reverts whatever it replaces** — and when the old CODE and its old TEST travel together, the tree is self-consistent at the wrong version and **no check in the repo can see it.**
✅ **THE RULES, and ledger rules 313 and 314 own them:**
**1.** **Every UPLOAD doc opens with STEP 0 — delete the superseded folder, by name.**
**2.** **Verify by CONTENT, never by filename**: *“open the file on GitHub and search for `<symbol>`”*. A filename and a commit message are not evidence about bytes.
**3.** **When two fixes are outstanding, ship ONE folder containing ALL the files and say “select all”.** Nothing to choose between is the only instruction that cannot be followed wrongly. ⚠️ This is about what a human drags out of a folder at midnight — it does not loosen “one PR per task”, which is about reviewable units in git.
**4.** **Never assume an upload landed.** `git fetch` and diff the symbol counts before reporting success.
**5.** 🆕 `[2026-09-21]` **Every content check tells Sam to open the file's `Raw` view first, then press Ctrl+F.** ~~"open the file on GitHub and search for `<symbol>`"~~ is not enough on its own. GitHub's normal code view only loads the lines currently on screen, so on a long file the browser's Ctrl+F cannot find text that is really there. On 2026-09-21 Sam searched `collect.py` (6,000+ lines) and `verify_nfl.py` for their markers and found neither, while `git fetch` showed all 7 uploaded files matched the delivered ones byte for byte (md5). The search failing was the viewer, not the upload. ➡️ **The authoritative check is still rule 4 (Claude fetches `main` and compares). Sam's Ctrl+F is a quick look, and it must be done on `Raw`.**

⚠️ **Tool output truncates around 1KB.** Page long tables, or rebuild locally and **verify with aggregate checksums.**

**Prune.** A doc that only accumulates is slower to use than no doc.

🔴 **AN ABSENCE IN A QUERY IS NOT AN ABSENCE IN THE WORLD.** **State what the query returned, then state separately what that does and does not prove.**
🆕 🔴 **AND A SUMMARY OF A QUERY IS NOT THE QUERY.** `[measured 8/21]` A fetch reported its own row count as **1000** while enumerating **816**. **Trust enumerated rows; never a fetched summary's count of them.**
🆕 🔴 **AND AN ENCODING IN A QUERY IS NOT A FACT ABOUT THE WORLD EITHER.** `[measured 8/21]` **The project's stated premise rested on reading the feed's goblin/demon flag as a price.** ⛔ **Fifth instance of the same failure family, and the most expensive.**

📐 **Record format: `8/10`, not `8-2`.** Hits over attempts, everywhere.

**Tags:** `[measured]` · `[observed]` · `[hypothesis]`.

---

## Revision log

### v5.7 — Sept 21 2026, evening: **a content check told Sam an upload had failed when it had landed**

- 🔴 **The delivery protocol's rule 2 ("search the file on GitHub for `<symbol>`") gave a false negative.** GitHub's code view only loads the part of a long file that is on screen, so Ctrl+F missed markers that `git fetch` showed were present. All 7 files of the red-runs fix matched the delivered copies by md5. ✅ **Rule 5 added: every content check goes through the `Raw` view.** No other part of this file was touched.

### v5.6 — Sept 21 2026, Monday sweep 🔴 **THE PITCHER DATABASE IS THIRTY-ONE SLATES BEHIND; CHECK 10 FAILED A THIRD TIME (TWELVE DOCS MISSING FROM THE LIST); AND THIS FILE'S OWN PRE-PUBLISH CHECK 21 STILL CARRIED THE PRE-8/26 BAND.**

- 🔴 **JOB 1 — `claude/mlb-pitcher-database.md` IS 31 COMPLETED SLATES, ~840 STARTS AND 32 DAYS BEHIND. AN INTERACTIVE SESSION MUST RUN THE RECIPE.** **Built through 8/20 on 3,838 starts; season population ≈ 4,678 through 9/20, bounded two enumerated ways (the 9/14 anchor + 109 games enumerated from `results/final.json.gz`; and 4,601 `min_ip`-filtered started rows in `data/latest/pitchers.json.gz`, a 77-row filter gap).** ✅ **Table internally sound (185 rows, `sum(n)` 3,403, min n 8). Acceptance test RE-DRIVEN, not re-read: the repo's per-start log through 8/20 with the sample SD reproduces Misiorowski 0.252/0.174 and Gilbert 0.289/0.132.** ⛔ **No statsapi population call; nothing rebuilt.**
- ✅ **CODE vs DOCS, BOTH DIRECTIONS — the code is CLEAN against the docs:** props regions `us,us2` with `us2` refreshes after the full pull (rule 48); `us_dfs` nowhere; three `PITCHER_MARKETS` (no `pitcher_outs_alternate`) and five batter markets; all 16 `card.py` model constants equal `claude/mlb-projection-model.md` v5.0; K/9 class band ±1.5; outs durability buckets `<14/14–16/16–18/18+`; `PRICE_FLOOR = -700` inclusive in `card.py` and `verify_card.py`; `PARLAY_BANDS` 1.80–2.20 / 3.00–6.00 with the 1.80 floor; `blend` the plain 50/50; T21/T22 shadow-only. ⚠️ **One cosmetic code note, reported not fixed: `card.py` `outs_k()`'s docstring still calls the pooled reference slope "0.525" (v4.0's; v5.0's is 0.5155) — prose only, not a shipped value.**
- 🔴 **THE DOCS WERE BEHIND THE CODE (OR EACH OTHER) IN 58 PATCHED PLACES ACROSS 18 DOCS — 21 DOCS EDITED IN ALL, COUNTING THE PITCHER DATABASE, THE REGISTER AND THIS FILE; COUNTED FROM THE PATCH LISTS, NOT ESTIMATED (check 28). FIXED, EACH STRUCK-AND-DATED IN PLACE:** this file (the doc-list descriptor of `claude/card-blueprint.md` and **pre-publish check 21 still said 1.8x–2.1x**); `claude/card-blueprint.md` (three live 2.1x band references, a "four-market" pull, the dead weather-blocker, PrizePicks listed as a PRICE source, and the outs matched-class axis the code has used since 8/23); `claude/mlb-alt-line-protocol.md` (two live 2.1x references; "odds only from Chrome"); `claude/mlb-data-stack.md` (nine lines — a struck 4-credit rate still quoted, the dead weather-blocker, "every `n` is the true full-season count", two surviving StatMuse "fitted variable" lines, a v4.0 pointer, a credential-in-a-doc rule citing the wrong ledger rule, "two markets per call max"); `claude/gizmos-picks-spec.md` (six stale lines incl. "the Sept 1 re-fit is owed" and "neither a browser nor network access"); `claude/mlb-opponent-database.md` (v5.0 "shipped 09-01" → 09-11; a v4.0 slope in a live bound, labelled); `claude/backtest-results.md` and `claude/mlb-team-advanced.md` (v4.0 coefficients and a hits-allowed R² stated as current); `claude/multi-league-spec.md` (the CFBD-key question answered; football's three-book line flagged against the code's five); `claude/open-questions.md` (the `concurrency:` claim is true only for NON-MLB crons — verified in `collect.yml`; two answered items); `claude/audit-2026-09-19.md` / `-runs.md` ("nothing has ever been auto-repaired" and "probably NOT the Tests step" both superseded); `claude/audit-2026-09-20.md` (**title and intro were copied from `claude/update-schedule.md`**); `claude/repo-session-handoff.md` (a 2.1x band, a stale rule and cron count, two ⬜ items now landed); `claude/plan-1-8-completion.md` (the credit-balance watcher now exists; the possession "calendar gate" is an ordering bug); `claude/line-movement.md` (football news IS archived now; MLB is not); `claude/self-healing-architecture.md`, `claude/possession-validation.md`, `claude/repo-inventory-2026-09-19.md` (counts and prefixes).
- ✅ **CHECK 10 — twelve docs added to the list above; the task table re-stamped (five tasks, crons unchanged).**
- ⚠️ **TASK PROMPTS — REPORTED, NOT FIXED (an unattended run cannot call `update_trigger`):** the Monday-sweep and grading prompts STILL carry `1.8x–2.1x` (DOC DEBT since 2026-08-31, three weeks past its deadline; this run again did not enforce the tripwire and re-narrowed nothing); the daily-audit prompt's 8/31 debt is CLOSED (verified) but its rewrite added **"Expected ~250/day"** against a measured ~475/day — new DOC DEBT row. ⚠️ **Sam's own instructions box still says "the 1.8x-2.1x band"; only he can edit it.**
- ⏳ **LEFT FOR SAM, NOT GUESSED:** football's book set (his 8/27 "Hard Rock, FanDuel, DraftKings" vs the code's five-book filter); whether T-A–T-D should be folded into the register.
- ⚠️ **Method:** every doc `project_read` → verbatim content taken from the tool results (session and sub-agent transcripts, never retyped) → programmatic patch, every anchor ASSERTED to occur exactly once → `collections.Counter` line-loss check (every lost line an intended replacement) → `created_at` re-checked against `project_info` immediately before upload → `local_path` upload. ⛔ **NO GRADED ROW, MONEY ROW, DATED CARD TABLE, MODEL COEFFICIENT, TEST SPECIFICATION, PASS BAR OR VERDICT WAS ALTERED. `claude/pick-ledger.md` and `claude/calibration-accumulators.md` were read and NOT touched.** **Row controls: pitcher table 185 rows / `sum(n)` 3,403 before and after (data block byte-identical); opponent table unchanged (both edits outside it); `claude/owed-tests.md` 54 T-entry headers before and after.**

### v5.5 — Sept 14 2026, Monday sweep 🔴 **THE DRIFT RAN CODE-AHEAD-OF-DOCS ALL WEEK: `card.py` FIXED FOUR DEFECTS ON 2026-09-11 AND FOUR DOCS WERE STILL REPORTING THEM AS OPEN — ONE OF THEM WRITTEN *AFTER* THE FIX. THE PITCHER DATABASE IS NOW TWENTY-THREE SLATES BEHIND.**

- 🔴 **JOB 1 — THE PITCHER DATABASE IS TWENTY-THREE COMPLETED SLATES AND ~622 STARTS BEHIND. AN INTERACTIVE SESSION MUST RUN THE RECIPE. THIS IS THE FIRST LINE OF THE REPORT FOR A REASON.** **`claude/mlb-pitcher-database.md` is built through the 8/20 slate on 3,838 starts against a season population of ~4,460 through 9/12 — 25 days stale.** ✅ **THE PUBLISHED TABLE ITSELF RE-VERIFIED CLEAN: 185 rows · `sum(n)` 3,403 · min n 8, max 27 · ZERO rows below n≥8 · no duplicate names · exactly 10 pipes on every row · both acceptance-test pitchers at their stated values (Misiorowski `0.252 / 0.174`, Gilbert `0.289 / 0.132`), so the sample-SD (n−1) convention is intact. It is not corrupt; it is OLD, and the gap has grown by six slates in a week.** ✅ **THE POPULATION WAS BOUNDED BEFORE IT WAS BELIEVED, FROM ENUMERATED ROWS ONLY, TWO INDEPENDENT WAYS: (1) the 2026-09-07 anchor of 4,308 through 9/6 — itself verified three ways — plus 76 games enumerated one slate at a time from the repo\'s own `data/<date>/results/final.json.gz` (9/7 = 11, 9/8 = 15, 9/9 = 15, 9/10 = 5, 9/11 = 15, 9/12 = 15, every one `n_final` equal to `n_games`) = 4,460. (2) `data/latest/pitchers.json.gz` (`pulled_at 2026-09-13T10:16Z`, `n_failed: 0`, `domain_violations: 0`) enumerates 4,382 started rows through 9/12 under its `min_ip >= 20` filter, home 2,194 / road 2,188 — a 78-row filter gap against route 1, against 70 at 9/6 and 56 at 8/20, which is the right direction and magnitude for September call-ups.** ✅ **Sanity bound: 622 starts over 23 slates is 27.0 a slate ≈ 13.5 games, inside the 5–15 game slates the repo recorded.** ⛔ **NO SEASON-POPULATION CALL WAS MADE AND NO FETCHED FIGURE IS ASSERTED.** ⚠️ **9/13\'s slate was played but its results snapshot is not yet in the repo (`n_final: 0` at the 9/13 10:14Z pull), so it is NOT counted — the true gap is one slate larger than the figure above.**
- 🔴 **THE WEEK\'S HEADLINE, AND IT IS RULE 61 INVERTED FOR THE SIXTH TIME: FOUR `card.py` DEFECTS WERE FIXED ON 2026-09-11 AND THE DOCS DID NOT HEAR.** `[every one verified 2026-09-14 from a fresh clone, in this same turn]` **The `in_band` / `parlay_rule` ceiling disagreement (open and re-reported since 8/26) — FIXED, `in_band` now reads `PARLAY_BANDS`. `price > -700` excluding exactly −700 — FIXED, `>=` in both floors. The three `v4.0` PROSE strings — FIXED, all three now read `MODEL_VERSION`. `calibration_warning`\'s hard-coded −25.9 / −18.0 — FIXED, `calibration_sentence()` builds from `data/latest/record.json` and fails to "not enough graded plays yet" rather than to a stale number.** 🔴 **AND THE WORST INSTANCE IS `claude/mlb-projection-model.md`, WHICH WAS CREATED ON 2026-09-11 AND ASSERTS TWO OF THOSE FOUR AS "STILL OPEN" — a doc that was stale on the day it was written.** ⚠️ **Its own CODE-DEBT block, five lines up, records the other two as fixed that same day.** ➡️ **`claude/mlb-alt-line-protocol.md`, `claude/gizmos-picks-spec.md` and `claude/multi-league-spec.md` carried the same dead claims. All four corrected.**
- 🔴 **`claude/gizmos-picks-spec.md` STILL SAID THE CARD RUNS v4.0 — three days after `MODEL_VERSION` was bumped — in the section that describes what the card computes.** ✅ **Struck in three places, and the prohibition was widened from "never quote a v4.0 coefficient" to "never quote a MODEL coefficient of any version," because naming the version is what let the doc go stale on the version instead of on the rule.** ✅ **Its `96 CREDITS PER PULL` was also arithmetic that contradicted itself — `3 × 2 × 15` is 90, and the deployed budget table in the same document says 90. Restated as the per-game RATE, which cannot go stale with the slate size.** ✅ **And its SIXTH cron count is struck without a seventh being written: `collect.yml` now carries `CRON COUNT: 43`, matching its own 43 entries, asserted by `test_cron_wiring.py`.**
- 🔴 **THE BAND CEILING WAS STILL 2.1x IN THE PLACES A CARD IS ACTUALLY WRITTEN FROM, EIGHTEEN DAYS AFTER SAM WIDENED IT.** **`claude/card-blueprint.md` STEP 6\'s HEADING, its "AT LEAST FIVE PAIRS" item 1, and its band ARITHMETIC; `claude/mlb-alt-line-protocol.md`\'s output format and its single-leg cut; `claude/today-shortlist.md`\'s band remedy; `claude/update-schedule.md` and `claude/football-todo.md` in passing.** ⚠️ 🔴 **THE 2026-09-12 SWEEP FIXED STEP 6\'s TABLE CELLS AND LEFT THE HEADING, ON THE EXPLICIT GROUND THAT THE ARITHMETIC IS "arithmetic about a decimal, not a statement of the live ceiling." THAT REASONING IS RECORDED AS WRONG: the block ends in an instruction to check each leg against 2.10 before pairing it, so at Sam\'s live 2.20 ceiling it REJECTS LEGAL PAIRS — a +115 leg at 2.15 now fits.** ✅ **Both re-solved at 2.20 (partner ceiling 1.925 ≈ −108; single-leg cut decimal ≥ 2.20 ≈ +120), with the historical Bradish example kept intact as history.**
- 🔴 **TWO CLOSED OWED TESTS WERE STILL CALLED OPEN IN FIVE MORE PLACES, THIRTEEN DAYS AFTER THEY CLOSED — AND FOUR OF THE FIVE ARE DOCS THE 2026-09-07 SWEEP ALREADY "FIXED".** **T3 (outs distribution, CLOSED-PASSED 2026-09-01) in `claude/pitcher-reliability.md`\'s METHODS block — the half a REBUILDER reads, where 9/7 fixed only card item 6 — and in `claude/pitcher-tier-finding.md`. T1 (CLOSED-FAILED) in `claude/times-through-order.md`\'s BODY, where 9/7 fixed only the "Open" section, in `claude/mlb-opponent-database.md`, and in `claude/today-shortlist.md`.** ➡️ 🔴 **THE LESSON IS THE ONE THIS PROJECT ALREADY WROTE DOWN AND DID NOT APPLY: AN ENUMERATED LIST OF INSTANCES IS NOT A SWEEP. Fix the DOC, not the citations you happened to list.**
- 🔴 **THE REGISTER CONTRADICTED ITSELF ON WEATHER, AND IT HAD FOR THREE WEEKS.** **`claude/owed-tests.md` T13b says "NO HISTORICAL WEATHER SOURCE EXISTS IN THIS PROJECT\'S STACK" while T32, in the same file, records `collect.py` mode `weather` pulling Open-Meteo archive temperature and wind on 1,971 of 1,973 games since 2026-08-25.** `[verified 2026-09-14 from a fresh clone: the mode exists, `archive-api.open-meteo.com` is called, `data/latest/weather.json.gz` is live]` ✅ **The factual BLOCKER claim is corrected in T13b and in `claude/mlb-data-stack.md`\'s open hunt.** ⛔ **NOTHING ELSE IN THE ENTRY WAS TOUCHED: no specification, no pass bar, no status. T13b stays OPEN AND UNSPECIFIED, the standing wording "weather is UNTESTED, not a measured null" is unchanged, and the distinction that matters is stated — the weather work that exists is on GAME TOTALS (T31/T32/T33) and has NEVER touched a starter\'s strikeouts or outs.** **T-entry header count 47 before and 47 after.**
- ✅ **OTHER CONTRADICTIONS FOUND AND FIXED, ONE LINE EACH:** **`claude/multi-league-spec.md`** was banner-marked **"PLAN ONLY. NOTHING IS BUILT"** while `cfb.py`, `nfl.py`, `card_fb.py` and live `data/ncaaf/latest/` and `data/nfl/latest/` boards sit on main — and it still carried a **DOC DEBT item that the repo\'s `CLAUDE.md` closed on 8/27**, a **600-credit "daily cap"** that is now only the FLOOR of `daily_allowance()`, and a credit figure that **did not reconcile with itself** (20,000 − 18,786 is 1,214, not 464). **`claude/backtest-weather-homeaway.md`** stated in bold that **weather is NULL** — the named tripwire — and quoted an **UNSOURCED roof-venue list** its sibling doc strikes as unsourced. **`claude/backtest-hits-allowed.md`** said in two places that its count-vs-rate question was **"in NO entry of `claude/owed-tests.md`"** when it is **T20, and FIRST in that register\'s execution order**. **`claude/today-shortlist.md`**\'s stale-archive banner said the card was **"FOUR DAYS OLD"** and asserted a three-week-old ledger reading in the **present tense** — the exact failure the banner two paragraphs above it condemns. **`claude/pitcher-reliability.md`** still called Alcantara the **"deepest workload on the board"** three weeks after `claude/mlb-pitcher-database.md` struck the same claim (Yamamoto is deeper). **`claude/mlb-opponent-database.md`** printed the **whole v4.0 K equation in a COPY-PASTEABLE CODE BLOCK** three lines above a note saying it is not authoritative on the formula. **`claude/mlb-data-stack.md`** carried a **hard-coded opponent league mean** three sections above its own ban on doing that — and its 2026-08-24 changelog **claims under GROUP H that the ±0.151/±0.189 venue terms were replaced by pointers. They were not.** **`claude/card-blueprint.md`** STEP 5 mandated a **FOUR-market pull** against the collector\'s three and against rule 61, which the same section records two screens below. **`claude/backtest-results.md` — the project's own ORIENTATION page — carried the bare tripwire phrase "Weather and parks null", an unlabelled v4.0 opponent slope and trailing-8 weight, a 12-of-40 shadow-ladder count the register now has at 25, and the struck "EVERY data path runs through the Chrome extension" diagnosis that cost a month of re-fits. `claude/mlb-team-advanced.md` carried an inline v4.0 opponent coefficient INSIDE the banner that forbids copying constants, the same dead weather blocker, and an 18.4-point QUALITY/FRINGE gap whose label is built from SEASON TOTALS and applied to outings inside that season — lookahead, the construction `claude/pitcher-tier-finding.md` records reversing a headline result once already.** **FOUR docs had duplicated `## Changelog` headings; merged.**
- ⚠️ 🔴 **REPORTED, NOT FIXED — A HEADLESS RUN CANNOT TEST OR PUSH CODE** (`github.com/smh0602/gizmos-picks`, fresh clone this turn): **nothing is open.** ✅ **Every code defect this table has carried since 8/24 is now closed — the band ceiling, the −700 floor, the v4.0 constants and stamp, the v4.0 prose strings, the calibration banner and the workflow cron count.** ⚠️ **`freshness.json`\'s `due_at` for the 9/14 MLB artifacts reads `2026-09-13T10:00Z` with `stale: false` at a 09:53Z converge pass — consistent with "the deadline this file satisfied" rather than the next one, and NOT reported as a defect because the contract is the repo\'s to define.**
- ✅ **CHECK 10 WAS FAILING AGAIN, ONE WEEK AFTER IT WAS FIXED: the doc list named 33 and the project holds 36.** `[verified against `project_info` in this same turn]` **`claude/self-healing-architecture.md`, `claude/halftime-markets-decision.md` and `claude/football-parlays-decision.md` were all created after the 9/7 sweep and none had reached this page.** ✅ **All three added.** ➡️ **A doc list is not fixed once; it goes stale at the rate new docs are written.**
- ✅ **THE TASK TABLE\'S COUNT WAS RIGHT FOR THE SECOND CONSECUTIVE WEEK — FIVE, and every cron unmoved since 9/7.** ⚠️ **One row\'s NAME had changed (the daily 15:30Z audit); the row is corrected and the standing rule held exactly as written — the cron is the authority, the name is prose.**
- 🔴 ⛔ **AND THE SWEEP\'S OWN PROMPT IS NOW A WEEK PAST A MISSED DEADLINE, AND THIS RUN AGAIN DELIBERATELY DID NOT ENFORCE IT.** **Its tripwire *"Does any doc state a pair target other than the 1.8x–2.1x band?"* would, taken literally, strike Sam\'s 2026-08-26 bands back OUT of the doc set.** ✅ **Nothing was re-narrowed: rule 28\'s 1.80–2.20 / 3.00–6.00 was read, confirmed, and used as the CORRECT side of every band fix above.** ⛔ **`update_trigger` needs human approval and an unattended run has nobody to give it. The DOC DEBT row stays.** ⚠️ **The grading prompt\'s own inline `1.8x–2.1x` (in its OUT OF BAND clause) is LIVE and would mis-classify a 2.15x slip — also still owed.** ⚠️ **Sam\'s own instructions box still describes `claude/card-blueprint.md` as "the process, and the 1.8x-2.1x band," and only he can edit it.**
- ⚠️ **Method:** every doc `project_read` → **the returned content taken VERBATIM (file results copied; inline results extracted from this session\'s transcript JSONL with a JSON walker, never retyped)** → programmatic patch with every anchor ASSERTED to occur EXACTLY ONCE before any write → `collections.Counter` line-loss check, every dropped line an intended replacement → upload with `local_path`, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT, GRADED ROW, MONEY ROW, DATED CARD TABLE, CALIBRATION FIGURE, BREAK-EVEN VALUE, TEST SPECIFICATION, PASS CRITERION OR CLOSED VERDICT WAS ALTERED IN ANY DOC — several coefficients were RELOCATED behind pointers, none was edited.** ✅ **Row-count controls run and reported: `claude/mlb-opponent-database.md` 30 team rows before and after; `claude/today-shortlist.md` 10 graded W/L markers before and after; `claude/owed-tests.md` 47 T-entry headers before and after.** ⛔ **`claude/pick-ledger.md` AND `claude/mlb-pitcher-database.md` WERE READ AND NOT TOUCHED.** **SEVENTEEN docs edited — and the count is stated AFTER counting the rows, because check 28 has caught this exact sentence before, INCLUDING in the first draft of this very entry, which said "fourteen" over fifteen names and then fifteen over seventeen: `claude/backtest-results.md`, `claude/mlb-team-advanced.md`, `claude/mlb-projection-model.md`, `claude/mlb-alt-line-protocol.md`, `claude/gizmos-picks-spec.md`, `claude/card-blueprint.md`, `claude/pitcher-tier-finding.md`, `claude/backtest-weather-homeaway.md`, `claude/backtest-hits-allowed.md`, `claude/today-shortlist.md`, `claude/multi-league-spec.md`, `claude/pitcher-reliability.md`, `claude/times-through-order.md`, `claude/mlb-opponent-database.md`, `claude/mlb-data-stack.md`, `claude/owed-tests.md`, and this one.**

### v5.4 — Sept 7 2026, Monday sweep 🔴 **THE DOC LIST HAD BEEN FAILING CHECK 10 FOR AT LEAST A WEEK WHILE CLAIMING TO PASS IT; TWO OWED TESTS THAT CLOSED ON 9/1 WERE STILL CALLED OPEN IN FOUR DOCS; AND THE PITCHER DATABASE'S "A SCHEDULED RUN CANNOT FIX THIS" IS TOO BROAD.**

- 🔴 **JOB 1 — THE PITCHER DATABASE IS SEVENTEEN COMPLETED SLATES AND 470 STARTS BEHIND. AN INTERACTIVE SESSION MUST RUN THE RECIPE.** **`claude/mlb-pitcher-database.md` is built through the 8/20 slate on 3,838 starts against a season population of 4,308 through 9/6.** ✅ **THE PUBLISHED TABLE ITSELF VERIFIES: 185 rows · `sum(n)` 3,403 · ZERO rows below n≥8 · no duplicate names · 10 pipes on every row · both acceptance-test pitchers present at their stated values (Misiorowski 0.252/0.174 · Gilbert 0.289/0.132), so the sample-SD (n−1) convention is intact. It is not corrupt; it is OLD, and ten slates has become seventeen.** ✅ 🔴 **THE POPULATION IS VERIFIED, NOT MERELY BOUNDED, THREE INDEPENDENT ENUMERATED WAYS: `claude/mlb-opponent-database.md`'s 2026-09-06 member-for-member verification (4,278 through 9/5, closed three ways) plus the 9/6 slate enumerated at 15 `Final` games from the repo's own `results/final.json.gz` = 4,308; `data/latest/pitchers.json.gz` enumerating 4,221 started rows through 9/6 under `min_ip >= 20`, whose 3,775-vs-3,838 gap at 8/20 shows the filter dropping 63 there and 87 here; and home 2,112 / road 2,109 across those rows.** ✅ **Bounded before it was believed: 470 starts over 17 slates is 13.8 games a slate, inside the 9–16 game slates the repo recorded.** ⛔ **No season-population call was made and no fetched figure is asserted.**
- 🔴 🆕 **AND THE PITCHER DATABASE'S OWN BLOCKER CLAIM IS TOO BROAD — QUALIFIED, AND IT IS THE ERROR SHAPE v5.3 NAMED.** `[measured 2026-09-07]` **`data/latest/pitchers.json.gz` carries the full per-start log — `d`, `o`, `h`, `gs`, `outs`, `k`, `np`, `bf` — and 4,221 started rows were enumerated from it headless in this run. That is this table's input, and it was reachable.** ⚠️ 🔴 **BUT NOT THE SAME POPULATION: the file applies `min_ip >= 20`, a season-total sample filter. At n≥8 it drops only the OPENERS this doc already excludes — but it is a real difference and any rebuild from it must say so.** ⛔ **AND IT IS NOT A SUBSTITUTE WHERE A POPULATION-SCALED CONSTANT IS INVOLVED: `claude/mlb-opponent-database.md`'s centering constant is defined on the UNFILTERED population and the runner already computes a DIFFERENT one from this very file. THAT DOC IS RIGHT TO SAY ITS OWN REBUILD NEEDS THE RECIPE, AND IT WAS NOT "CORRECTED".** ➡️ **The distinction is the finding: one table can be rebuilt headless with a caveat, the other cannot be rebuilt headless at all.** ⛔ **Neither was rebuilt — a sweep verifies and reports.**
- 🔴 **CHECK 10 WAS FAILING AND THIS FILE CLAIMED IT PASSED. THE DOC LIST NAMED 21 OF 33 DOCS.** `[verified 2026-09-07 against `project_info` in the same turn]` **Twelve were missing, and the worst is `claude/gizmos-picks-spec.md` — named as REQUIRED READING in Sam's own instructions box and absent from this file's list entirely.** **The other eleven are the Gizmo's Picks and football workstreams' docs.** ⛔ **The 2026-08-31 v5.0 claim that the list "was re-verified against `project_info` in the same turn and matches" was FALSE WHEN WRITTEN — `gizmos-picks-spec.md` had existed since 8/22.** ✅ **All twelve added; the false claim struck in place.**
- ✅ **THE TASK TABLE'S COUNT WAS RIGHT FOR THE FIRST TIME IN FOUR WEEKS — FIVE, verified against `list_triggers` in this turn. THE FIFTH ROW'S CRON WAS NOT.** **`Sports + sportsbook board check` now reads `0 12 * * 1-5` (weekdays), not the `0 12 * * 1` recorded 2026-09-01 17:00Z.** ✅ 🔴 **AND THE 9/1 READING WAS CORRECT WHEN MADE — a Mondays-only cron produces exactly the Monday 9/7 `next_run_at` it recorded and CANNOT produce the FRIDAY 2026-09-04T12:28Z firing now in `last_run`. The cron changed; nobody misread it.** ➡️ **The "read the cron, never the name" lesson survives; what is added is that a STAMPED ROW IS A CLAIM ABOUT A MOMENT.**
- ✅ **FOUR DOCS WERE STILL CALLING TWO CLOSED OWED TESTS OPEN, SIX DAYS AFTER THEY CLOSED. ALL FOUR FIXED:** **T3 (outs distribution — CLOSED-PASSED 2026-09-01, all three criteria)** was called *"never fitted"* / *"should be tested at the monthly re-fit"* in **`claude/card-blueprint.md`**, **`claude/mlb-alt-line-protocol.md`**, **`claude/pitcher-reliability.md`** and **`claude/mlb-pitcher-database.md`**; **T1 (`E[outs]` in the K model — CLOSED-FAILED 2026-09-01)** was listed as a live open question in **`claude/times-through-order.md`**. ⚠️ **Every fix carries the measured result and the ~8%-fatter-tails caveat rather than a bare "it passed".**
- 🔴 **`claude/card-blueprint.md` STEP 2's `IN THE MODEL` WEIGHT COLUMN WAS STILL v4.0 — ALL FIVE ENTRIES — SIX DAYS AFTER v5.0 PUBLISHED.** ⛔ **This file's own standing rule, broken in the table a card builder reads first.** ✅ **Struck; every weight now points at `claude/mlb-projection-model.md`, with the three traps named inline (centre on the constant the opponent doc states · home/road is TWO-SIDED · the outs `k` is TIERED and the pooled slope is not shipped).** ⚠️ **Impact stated honestly: worst case 0.053 K and 0.133 outs. Last month's constants, and it barely matters — but "small" is not "right".**
- ⚠️ 🔴 **REPORTED, NOT FIXED — A HEADLESS RUN CANNOT TEST OR PUSH CODE** (`github.com/smh0602/gizmos-picks`, fresh clone this turn): **`card.py` still declares `MODEL_VERSION = "v4.0"` and its model block still matches v4.0 constant for constant — the CODE-DEBT block in `claude/mlb-projection-model.md` is unactioned six days on;** **`in_band` is still computed against `TARGET = 2.10` while `PARLAY_BANDS[2]` is `(1.80, 2.20)` and the `parlay_rule` prose says 1.8x–2.2x — a THREE-way disagreement inside one file, open since 8/26 and re-reported by every run since;** **`floor_ok` and `clears_price_floor` still write `price > -700`, excluding EXACTLY −700 where Sam's rule is "−700, nothing shorter";** **and `.github/workflows/collect.yml`'s header still says "the count is 16" where the file now enumerates THIRTY-NINE cron entries — the 8/31 fix was correct when made and the football crons have since more than doubled it.**
- ✅ **ONE LONG-STANDING REPORTED DEFECT IS GONE AND IS RECORDED AS CLOSED: `card.py`'s `calibration_warning` no longer hard-codes −25.9 / −18.0.** **`calibration_sentence()` now builds from `load_calibration('data/latest/record.json')` and FAILS TO SILENCE — "not enough graded plays yet … treat every one as unproven" — rather than to a stale number.** **Every grading run since 8/24 reported this; it is fixed.**
- ✅ **RE-CHECKED AGAINST THE CODE AND CLEAN, BOTH DIRECTIONS:** **the props pull is `REGIONS_FULL = "us,us2"` with `props_regions(kind)` dropping to `us2` only AFTER the day's full pull is stored — which is the design, matching ledger rule 48 and `claude/update-schedule.md`;** **`us_dfs` appears nowhere;** **`PITCHER_MARKETS` is the three the docs say (`3 × 2 = 6` credits per game, matching `claude/card-blueprint.md` and `claude/mlb-data-stack.md`) and `BATTER_MARKETS` is five;** **`blend = 0.5 * model + 0.5 * raw` is the plain 50/50 with T21/T22 carried only in the shadow `carried` column;** **the matched class is `±1.5` season K/9 and the durability axis is trailing-outs point-in-time;** **`PARLAY_BANDS` is `{2: (1.80, 2.20), 3: (3.00, 6.00), 4: (3.00, 6.00)}` and `TOP10_PRICE_FLOOR` is −400, both matching ledger rule 28 and Sam's 8/26 instruction.** ⛔ **No LIVE `regions=us2` instruction and no stale per-game credit figure was found in any doc — the 8/24 sweep's repair has held for two weeks.**
- 🔴 ⛔ **AND THE SWEEP'S OWN PROMPT IS STALE IN THE WAY `claude/owed-tests.md` PREDICTED, ON THE DEADLINE IT NAMED.** **The DOC DEBT row filed 2026-08-31 gave a deadline of BEFORE 2026-09-07 for the Monday sweep prompt's tripwire *"Does any doc state a pair target other than the 1.8x–2.1x band?"* — because as written it instructs the run to strike Sam's 2026-08-26 two-leg 2.2x ceiling and the 3x–6x multi-leg band back out of the docs.** ⛔ **THE DEADLINE WAS MISSED. THE PROMPT STILL CARRIES IT, AND THIS RUN DELIBERATELY DID NOT ENFORCE IT.** ✅ **Nothing was re-narrowed: `claude/pick-ledger.md` rule 28 and `claude/card-blueprint.md` STEP 6 were read, confirmed correct at 1.80–2.20 / 3.00–6.00, and LEFT EXACTLY AS WRITTEN.** ➡️ **`update_trigger` needs human approval and an unattended run has nobody to give it, so the row stays in DOC DEBT — but it is now a MISSED deadline, not a pending one, and the next interactive session should treat it as the first item.** ⚠️ **THE SAME APPLIES TO SAM'S OWN PROJECT INSTRUCTIONS BOX, WHICH ONLY HE CAN EDIT: it still describes `claude/card-blueprint.md` as "the process, and the 1.8x-2.1x band."**
- ⚠️ **Method:** every doc `project_read` → **the returned content taken VERBATIM (no retype)** → programmatic patch with every anchor ASSERTED to occur EXACTLY ONCE before any write → `collections.Counter` line-loss check, every dropped line an intended replacement → `project_info` `created_at` compared immediately before upload → upload with `local_path`, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT, GRADED ROW, MONEY ROW, DATED CARD TABLE, CALIBRATION FIGURE, BREAK-EVEN VALUE, TEST SPECIFICATION OR PASS CRITERION WAS ALTERED IN ANY DOC. `claude/pick-ledger.md`, `claude/owed-tests.md` and `claude/mlb-opponent-database.md` WERE READ AND NOT TOUCHED.** **Six docs touched: `claude/mlb-pitcher-database.md`, `claude/card-blueprint.md`, `claude/mlb-alt-line-protocol.md`, `claude/pitcher-reliability.md`, `claude/times-through-order.md` and this one.**

### v5.3 — Sept 1 2026, 17:20Z 🔴 **THE RE-FIT RAN. v5.2 AND v5.1 WERE BOTH WRONG THAT IT COULDN'T, AND SO WAS EVERY SCHEDULED RUN BEFORE THEM.**

- 🔴 **SAM CALLED IT AND HE WAS RIGHT.** **Two entries on this page, written hours apart, both said the re-fit could not run headless. Both were wrong, and the error was not about the browser — it was about the DATA.**
  - ⚠️ **The browser half was worth checking and I had not checked it.** ✅ **Checked now: Chromium and Playwright ARE installed in the scheduled container and DO launch.** ⛔ **But the proxy refuses `CONNECT` to `statsapi.mlb.com` and `baseballsavant.mlb.com` (`ERR_TUNNEL_CONNECTION_FAILED`) while tunnelling to github fine — so THE RECIPE genuinely cannot run here. That half of the excuse was true.**
  - 🔴 **THE DATA HALF WAS NOT TRUE, AND IT IS THE ONE THAT MATTERED.** **`data/latest/pitchers.json.gz` in `github.com/smh0602/gizmos-picks` — a repo these scheduled runs were ALREADY CLONING — carries the collector's own nightly statsapi pull: `pulled_at 2026-09-01T10:06:38Z`, `n_failed: 0`, `domain_violations: 0`, and 4,056 season starts each carrying `h` (isHome), `np` (pitch count) and `bf` (batters faced).** ➡️ **That is the re-fit's input. It was reachable at 4am, and on 8/31, and on 8/24.**
  - ⛔ 🔴 **THE REASONING ERROR, NAMED: "the re-fit needs THE RECIPE" was read as "the re-fit needs a browser."** **THE RECIPE is one way to GET the start table, not the definition of it.** ➡️ **STANDING RULE: before declaring a headless run blocked on data, `ls data/latest/` in the repo and read what is there. One command, zero credits.** ⚠️ **This is a new shape of the project's oldest failure — a fact about ONE ROUTE written down as a fact about the WORLD — and this time it cost a month of re-fits rather than one card.**
- ✅ 🔴 **v5.0 OF THE MODEL IS PUBLISHED IN `claude/mlb-projection-model.md`.** **Fitted on 4,056 season starts through 8/31, 3,089 training rows after v4.0's own filters (≥3 prior starts, opponent ≥20 prior starts faced), every predictor AND every grouping variable point-in-time — tightened so rows sharing a DATE cannot see each other either.** **K: 4.9292 / 0.6653 / 0.5803 / ±0.1449, R² 0.1563. Outs: 15.9121 / tiered k 0.629-0.647-0.361 / 0.0366 / ±0.1994, R² 0.1614.** ⛔ **The coefficients are NOT restated anywhere but that doc — this file's own standing rule.**
- ✅ **AND THE HEADLINE IS HOW LITTLE MOVED: every v4.0 coefficient reproduced within thousandths on an independently-sourced, 236-start-larger sample.** ⚠️ **13 of `card.py`'s 16 model constants are now stale, but the worst pricing impact anywhere on a realistic board is 0.053 K and 0.133 outs.** ➡️ 🔴 **So the honest line is "the card is running last month's constants and it barely matters," NOT "the card is wrong." The CODE-DEBT block in the model doc says exactly that, with the arithmetic.** ⛔ **The re-fit is still not COMPLETE until `card.py` is updated — a headless run cannot push or test.**
- ✅ **TWO OWED TESTS CLOSED, EACH RUN ONCE, EXACTLY AS PRE-REGISTERED:**
  - 🔴 **T1 — FAILED.** `E[outs]` in the strikeout model: **t = +3.39 but ΔR² = +0.00313 against a +0.005 bar.** **The bar was a conjunction and both halves were required.** ⚠️ **Worth recording that a t of 3.39 is exactly what v3.4 would have shipped as a find — the pre-registration is the only thing separating them.**
  - ✅ **T3 — PASSED on all three criteria.** The outs distribution **is** Normal(mu, cvO × season mean outs): mean +0.0543, SD 1.0772, q10 −1.3528, q90 +1.3356. 🔴 **Every outs probability this project has ever quoted rested on that assumption and it holds.** ⚠️ **Tails ~8% fatter than Normal — inside the bar, worth naming on a far alt rung.**
- ✅ **ALL FOUR ESTABLISHED NULLS RE-RUN AS REGRESSION TESTS AND ALL HELD** — opponent→outs (t=−0.90), handedness→K (t=+1.09), prev-start-outs with pitch count in (t=+0.37), rest-days (t=−3.55, still significant-negative-confounded, still not priced). **None came alive.** ⚠️ **The register's standing warning that several nulls "were declared on the partial log and never re-run" is discharged for these four.**
- ✅ **THE FIVE-SPEC ROBUSTNESS SWEEP RE-RUN IN FULL** — pitch count significant in all five (t 3.86–6.73), prev-start outs in none (|t| ≤ 0.88), `k` stable 0.480–0.542. **It agrees with v4.0's sweep spec-for-spec, including the R² ordering.**
- ✅ **TWO ITEMS THE MODEL DOC HAD FLAGGED FOR THIS EXACT RE-FIT ARE CLOSED:** **the tier cut-points did NOT move** (terciles now 15.250 / 16.875; v4.0's 15.25 / 17.0 split the sample 32.7/34.0/33.3, so they were kept — the tercile is a diagnostic and was NOT adopted), and **the mid-tier R² was NOT a dropped digit** (re-fits at 0.0196). 🔴 **v4.0's refusal to "fix" that number by inference is vindicated.**
- ✅ **CHECK 18 SATISFIED:** numpy `lstsq` re-solved independently by hand-written Gauss-Jordan normal equations — agreement to **1.6e−14** (K) and **3.1e−14** (outs) on every β.
- ⚠️ 🔴 **THE ONE HONEST WEAKNESS, STATED UP FRONT RATHER THAN BURIED: the source applies `min_ip ≥ 20`, a SEASON-TOTAL sample filter.** **Every predictor and every grouping variable is point-in-time; SAMPLE MEMBERSHIP is not.** ➡️ **An interactive session with a browser should re-fit via THE RECIPE and compare.** ⚠️ **Direction of evidence: v4.0's coefficients, fitted with no such filter, reproduce here to thousandths — which is what a materially biasing filter would not do.**
- ⚠️ **Also recorded so it is not mistaken for the team-assignment trap: 12 rows carry `opponent == the pitcher's own team`. All 12 are mid-season TRADES** (`team` is his current club, `o` is per-start; every flagged start falls before his last). ⛔ **`team` is not a point-in-time field and is used nowhere in the fit.**
- ⚠️ **Method:** unchanged from v5.2 — `project_read` → verbatim transcript extraction → uniqueness-asserted programmatic patch → `collections.Counter` line-loss check → `created_at` compared immediately before upload → `local_path` upload. ⛔ **In `claude/owed-tests.md` the T-entry header count was 39 before and 39 after; no specification, pass criterion or closed verdict was altered, and the superseded 08:21Z brief is STRUCK, not deleted.** **Docs touched this pass: `claude/mlb-projection-model.md`, `claude/owed-tests.md`, `claude/mlb-data-stack.md` and this one.**

### v5.2 — Sept 1 2026, 17:00Z ⛔ **ITS CENTRAL CLAIM IS STRUCK BY v5.3 ABOVE — THE RE-FIT COULD RUN AND DID. The rest of the entry stands.** (a SECOND firing of the monthly task, hours after the first; ~~the re-fit still did not run~~, and three things the 4am run could not have known are now measured)

- ⚠️ **THIS IS THE MONTHLY TASK FIRING A SECOND TIME ON THE SAME DAY.** **The scheduled 08:00Z firing completed and wrote v5.1 and the register's READY-TO-RUN BRIEF at 08:19–08:21Z; `list_triggers` shows this run's `fired_at` as 2026-09-01T16:58Z with the next scheduled firing 2026-10-01.** ✅ **v5.1's work was NOT redone and NOT re-litigated — no entry was reopened, no counter moved, no specification touched.** ➡️ **This entry records only what was not knowable at 08:21Z.**
- 🔴 ⛔ ~~**THE RE-FIT STILL DID NOT RUN, AND THE REASON IN THE DOCS WAS SLIGHTLY WRONG.**~~ **STRUCK 17:20Z — IT RAN. See v5.3. The paragraph below was right that statsapi's reachability had been mis-stated and wrong about the conclusion it drew from it.** **STEP 0 answer is unchanged — NO browser, so no RECIPE, so no fit** — ⛔ **and no substitute dataset was improvised.** **But v5.0's blanket "`statsapi.mlb.com` IS UNREACHABLE FROM A SCHEDULED CLOUD RUN" is FALSE as written: `WebFetch` reached it fine this run.** ✅ **Struck and corrected above.** ➡️ **The conclusion holds for a better-stated reason: the only route a scheduled run has to statsapi is the one this project bans for data. That distinction matters because "unreachable" invites a future session to declare the blocker gone the day the egress opens.**
- 🔴 🆕 **A FIFTH statsapi LIE MODE, AND IT IS THE ONE WITH NO TELL: A COMPLETE, WELL-FORMED, DOMAIN-VALID RESPONSE THAT IS SIXTEEN HOURS STALE.** `[measured 2026-09-01 17:00Z]` **A `WebFetch` of the 9/1 schedule returned 15 games and all 15 matchups exactly right, and five of thirty probable pitchers at their 00:29Z values** — two of them confident wrong-looking NAMES (Arrighetti for Toronto, Prielipp for Minnesota), three of them silently blank. ⛔ **Every check this project owns PASSED: right count, right identities, right shape, and no value impossible in its domain — because a stale value is a real value.** 🔴 **THE FIRST READ WAS "WebFetch FABRICATED PITCHER NAMES" AND IT WAS WRONG; that sentence was one step from being written down as a sixth fabrication finding.** ✅ **The control that caught it is new and free: the collector's `data/<date>/schedule/*.json.gz` series is append-only at ~15-minute resolution, so any statsapi response can be DATED by finding the snapshot it matches — all five disputed values matched `0029.json.gz` exactly and none matched `1621.json.gz`.** ➡️ **Recorded in `claude/mlb-data-stack.md` as lie mode 5 plus the snapshot-archive staleness oracle.** ⛔ **This does not soften the `WebFetch` ban; it is a second, independent reason for it.**
- 🔴 🆕 **THE BATTER FIVE-BOOK FIX LANDED — AND FANDUEL IS NOT IN IT.** ✅ **The 15:37Z audit run had already closed the acceptance test in `claude/update-schedule.md`, and this run reproduced it independently from the artifact: `props-batter/1113.json.gz` is `regions: 'us,us2'`, `credits_used: 150`, `n_events: 15`, and `5 × 2 × 15 = 150` exactly.** 🔴 **BUT VERIFYING THE `regions` STRING IS NOT VERIFYING THE BOOKS: counting bookmakers inside the two snapshots — same minute, same regions, same 15 events, five seconds apart — `props-pitcher` returned 15 books with `fanduel` on 15/15 events, and `props-batter` returned 13 books with `fanduel` on 0/15.** ⛔ **Stated as a fact about the QUERY, not the world — but the same-minute contrast rules out the region set, the spend and an outage.** ➡️ **The hitter board is a FOUR-book board (DraftKings 15/15, BetMGM 14/15, Caesars 13/15 — all three at zero on every prior day), and `claude/update-schedule.md` now carries it, with the acceptance test permanently extended to COUNT THE BOOKS PER MARKET FAMILY after `regions` and `credits_used`.**
- ✅ **AND ONE THING THAT LOOKS LIKE THE OLD DEFECT AND IS NOT: the evening `us2` props pulls are the DESIGN.** `props_regions(kind)` returns the cheap region set once the day's full pull is stored, which is what the schedule table specifies. ⛔ **Written down so a future sweep grepping the snapshots for `us2` leaves them alone.**
- ⚠️ 🔴 **RE-CONFIRMED AT THE SOURCE AND STILL OPEN — REPORTED, NOT FIXED (a headless run cannot test or push):** **`card.py`'s model block still matches v4.0 on every constant** (so the code-debt step in the register's brief is still the right shape); **`in_band` is still computed against `TARGET = 2.10` while `PARLAY_BANDS[2]` is `(1.80, 2.20)` and `parlay_rule` prose says 1.8x–2.2x — a THREE-way disagreement inside one file**; **`floor_ok` / `clears_price_floor` still write `price > -700`, excluding exactly −700**; **`calibration_warning` still quotes −25.9 / −18.0.** 🔴 **AND THE STALE CALIBRATION LINE IS NOT A LATENT BUG — IT SHIPPED IN TODAY'S LIVE CARD:** `picks/2026-09-01.json`, generated 14:08Z, carries *"Only the 70-80% band has a record that supports its own number"* when `claude/pick-ledger.md` has that band at **−1.3 at n=23**. ⛔ **The two stale numbers are the smaller half; the claim built on them is now false on the ledger's own figures, and it has gone out on every card since the 8/22 grading.**
- ⚠️ **Method:** every doc `project_read` → **the returned content extracted VERBATIM from this session's transcript JSONL with a JSON walker, never retyped** → programmatic patch with every anchor ASSERTED to occur exactly once → `collections.Counter` line-loss check (zero original lines lost in any of the three) → fresh `project_read` compared immediately before upload → upload with `local_path`, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT, GRADED ROW, MONEY ROW, DATED CARD TABLE, CALIBRATION FIGURE, TEST SPECIFICATION OR PASS CRITERION WAS ALTERED IN ANY DOC. `claude/owed-tests.md` WAS NOT TOUCHED AT ALL — the 08:21Z brief stands as written.** **Three docs touched: `claude/mlb-data-stack.md`, `claude/update-schedule.md` and this one.**

### v5.1 — Sept 1 2026, 4am ET monthly re-fit firing (headless — THE RE-FIT DID NOT RUN; the register was verified, the brief was written, and the code moved ahead of one more doc overnight)

- 🔴 **THE RE-FIT DID NOT RUN AND NEEDS AN INTERACTIVE SESSION. THAT IS THE CORRECT OUTCOME, NOT A FAILURE.** **The full-season start table comes only from THE RECIPE, which needs Claude in Chrome; a scheduled run has none.** ⛔ **No substitute dataset was improvised — no StatMuse rebuild, no `claude/data-starts-h.csv.md` restore, no pitch-count collection (it is already in every row).** ✅ **Instead: every entry in `claude/owed-tests.md` was re-read against its own wording (NO quiet re-specification found), every sample-size trigger was checked against the ledger and the accumulators (NONE met — T4 17/30 · T11 25/40 · T15 31/100 · T23/T24 5/30 · T6 1/10 · T19 1/10 · T17 1/40 blocked · T25 0 joined), blocked entries were reported as blocked (T13b weather — no source; T17 — no captured prices; T10 — no key headless), and a READY-TO-RUN BRIEF with the execution order now sits ahead of Tier 1 in the register.** ➡️ **JOB 1 for the next interactive session: run THE RECIPE, then work the brief top to bottom.** ⚠️ **The pitcher and opponent databases are now ELEVEN completed slates behind (through the 8/31 slate, whose results snapshot was still pre-slate at this run — the ~24-start increment is schedule arithmetic, not enumerated).**
- ✅ **`card.py`'s MODEL BLOCK WAS COMPARED AGAINST `claude/mlb-projection-model.md` CONSTANT BY CONSTANT, from a fresh clone in this turn: EVERY ONE MATCHES v4.0 EXACTLY** — both intercepts, both trailing slopes and centerings, the opponent slope, both ± home/road terms, the pitch-count term and centering, the tier cut-points and all three tiered `k`s, the 8-start window, the 50/50 blend, and `cv` on the sample SD (n−1). ➡️ **So the day the re-fit publishes, the card is stale on every constant that moved — the brief's code-debt step exists for exactly that morning.**
- 🔴 **THE CODE MOVED AHEAD OF A DOC AGAIN, OVERNIGHT: `props_regions(kind)` — the batter five-book fix — LANDED ON MAIN 2026-08-31 23:21Z (commit `6a056c4`), and `claude/update-schedule.md` still carried a LIVE "the zip is still not uploaded" instruction this morning.** ✅ **Struck and corrected in place, with the acceptance test stated rather than skipped: by that doc's own rule a fix to a PAID pull is verified only by the NEXT stored snapshot's `regions` and `credits_used` — `data/2026-09-01/props-batter/` after ~11:04Z — and the verdict is assigned to whichever run reads it first (the 7:30am grading run or the 15:30Z audit).** ✅ **`claude/freshness-architecture.md` was already correct — the 8/31 evening interactive session wrote FIX 3 into it when it pushed the commit.**
- ✅ **ONE 8/31-REPORTED CODE DEFECT IS GONE: `.github/workflows/collect.yml`'s header ("the count is 16") now matches its own SIXTEEN enumerated cron entries** — the schedule was restructured in the 8/31 evening session (MLB crons now 10:04 / 11:04 / 14:04 / 20:04Z plus the every-3-hours backstop; the grading and audit prompts already point at the workflow file rather than restating times, so nothing else is owed on it).
- ⚠️ 🔴 **RE-CONFIRMED AT THE SOURCE THIS TURN AND STILL OPEN — REPORTED, NOT FIXED (a headless run cannot test or push code):** **`card.py`'s `in_band` is still computed against 2.10 while its own `parlay_rule` prose says 1.8x–2.2x** (an interactive session must pick one ceiling — open since 8/26); **`card.py` (`floor_ok`, `clears_price_floor`) and `verify_card.py` still exclude a rung priced at EXACTLY −700** where Sam's rule is "−700, nothing shorter"; **`card.py`'s `calibration_warning` still quotes −25.9 / −18.0 against the ledger's live −29.6 / −14.7.**
- ✅ **ALL FIVE STORED TASK PROMPTS WERE SCANNED FOR INLINE CONSTANTS THIS TURN.** **The three ⏳ task-prompt DOC DEBT rows filed 2026-08-31 are RE-CONFIRMED still owed against the stored prompts** (the Monday sweep's 1.8x–2.1x band tripwire — deadline BEFORE 2026-09-07 or it strikes Sam's 8/26 bands back out of the doc set; the grading prompt's inline band; the audit prompt's inline schedule and artifact count). ✅ **No NEW inline-constant debt was found: the two `regions=us2` strings in stored prompts are historical references inside lesson text, not live instructions.** ⛔ **An unattended run cannot edit a task prompt — the debt stays in `claude/owed-tests.md` DOC DEBT for an interactive session.**
- 🔴 **THE TASK TABLE WAS WRONG AGAIN WITHIN HOURS OF ITS 8/31 RE-STAMP — a FIFTH task (Sam's career-project job-board check, created 2026-09-01 01:53Z) existed by this firing.** ✅ **Row added, stamp re-dated, verified in the same turn (check 10 / check 25).**
- ⚠️ **Method:** every doc edited by `project_read` → **the tool result taken VERBATIM (file results `cp`'d; inline results extracted from this session's transcript JSONL with a JSON walker, never retyped)** → programmatic patch with anchors ASSERTED to occur EXACTLY ONCE → `collections.Counter` line-loss check → fresh `project_read` compared immediately before upload → upload with `local_path`, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT, GRADED ROW, MONEY ROW, DATED CARD TABLE, CALIBRATION FIGURE, TEST SPECIFICATION OR PASS CRITERION WAS ALTERED IN ANY DOC.** **Three docs touched this run: `claude/owed-tests.md` (the brief — pure insertion), `claude/update-schedule.md` (the landed-fix correction) and this one.**

### v5.0 — Aug 31 2026, Monday sweep (the task table was wrong in the OTHER direction; three of Sam's own instructions had been live in the code for up to a week with no doc carrying them; and the pitcher database is ten slates behind)

- 🔴 **JOB 1 — THE PITCHER DATABASE IS TEN COMPLETED SLATES AND ~276 STARTS BEHIND. AN INTERACTIVE SESSION MUST RUN THE RECIPE.** **`claude/mlb-pitcher-database.md` is built through the 8/20 slate on 3,838 starts.** ✅ **THE PUBLISHED TABLE ITSELF VERIFIES: 185 rows, `sum(n)` 3,403, ZERO rows below n≥8, no duplicate names, and BOTH named rebuild acceptance-test pitchers reproduce to four decimals (Misiorowski 0.252 / 0.174 · Gilbert 0.289 / 0.132).** ⛔ ~~**`statsapi.mlb.com` IS UNREACHABLE FROM A SCHEDULED CLOUD RUN (`connect_rejected`, egress policy), so the season population was NOT fetched and is NOT asserted from a fetch.**~~ 🔴 **TOO BROAD — CORRECTED 2026-09-01 17:00Z. `WebFetch` to `statsapi.mlb.com` from a scheduled run SUCCEEDS** (`/api/v1/teams?sportId=1` returned 30 teams; the 9/1 schedule call returned the full 15-game slate). ✅ **What is closed is the CONTAINER'S OWN direct egress, and container `curl` is separately blocked AND prohibited.** ➡️ **The conclusion is UNCHANGED — the season population still was not fetched and still must not be — because the one route a scheduled run has is the route this project BANS for data. Only the reason is corrected. Detail in `claude/mlb-data-stack.md` lie mode 5.** ✅ **IT IS BOUNDED FROM ENUMERATED ROWS INSTEAD: 4,086 starts through 8/29 (the 8/30 grading run's standings-authority figure, W=L control 2,043/2,043, independently reproduced by 834 enumerated season values across 347 starters) PLUS 28 started pitchers enumerated one by one out of the repo's own `data/2026-08-30/results/final.json.gz` (14 games, 14 final) = **~4,114 through 8/30**.** **Gap ≈ 276 starts over 10 slates = 27.6 starts a slate ≈ 13.8 games a slate, which is inside the 12–17 game slates the repo actually recorded — the bound is plausible as well as arithmetically consistent.** ⛔ **8/31's 12 games are scheduled and unplayed (`n_final: 0`) and are NOT counted.**
- 🔴 **THE TASK TABLE WAS WRONG AGAIN, IN THE OPPOSITE DIRECTION, AND ITS OWN LESSON PREDICTED IT.** **`list_triggers` returns FOUR tasks, not the three this file has asserted since 8/24: a `Gizmo's Picks — daily automation audit` was created 2026-08-28 at daily 15:30Z and NO PROJECT DOC KNEW IT EXISTED.** ✅ **Row added, stamp re-dated, verified in the same turn (check 10 / check 25).** ➡️ **8/24 struck a FOUR that had become THREE; 8/31 strikes a THREE that has become FOUR. The count is not the thing to maintain — the RE-READ is.**
- 🔴 **RULE 61'S FAILURE MODE INVERTED FOR THE FIFTH TIME, AND THIS TIME IT IS THREE OF SAM'S OWN INSTRUCTIONS AT ONCE.** **All three were live in `card.py` and in the repo's `CLAUDE.md`, and NOT ONE had reached a project doc:**
  - **2026-08-26 — THE PARLAY BANDS: two-mans 1.8x–2.2x, three- and four-mans 3x–6x.** **Every doc in this set still said the band is 1.8x–2.1x and that pairs are the only ticket shape.** ✅ **Swept into THE PAYOUT BAND above, `claude/pick-ledger.md` rule 28, `claude/card-blueprint.md` STEP 6 and `claude/what-every-file-does.md`.**
  - **2026-08-25 — THE −700 FLOOR NO LONGER GATES THE BOARD.** Sam: *"we need to also be allowing users to be able to bet on any props that are under our −700 odd threshold in the gizmos picks tab, we aren't building this just for me anymore."* ➡️ **The floor is NOT deleted — its SCOPE NARROWED. It still gates PAIRING and the starred alt rung; it no longer gates DISPLAY, and every below-floor row is shown and LABELLED.**
  - **2026-08-26 — A TOP-10-OF-THE-DAY LIST WITH ITS OWN −400 PAYABLE FLOOR**, deduped to one row per player, ranked by likelihood among rows a person would actually be paid on. **Sam's own number, like the −700 and the 1.8x.** ⚠️ **And a LINE rule that is not a price rule: BATTER ALTERNATE LINES ARE NEVER CARDED — a −300 alternate is still an alternate.**
- ⚠️ 🔴 **REPORTED, NOT FIXED — A HEADLESS RUN CANNOT TEST OR PUSH CODE** (`github.com/smh0602/gizmos-picks`):
  - 🔴 **THE BATTER SIDE IS *STILL* TAKING THE ONE-REGION PULL, AND IT HAS NOW MISPRICED A THIRD AND FOURTH CARD.** `claude/freshness-architecture.md` and `claude/update-schedule.md` both recorded this on 8/30 and BOTH ARE ACCURATE — the patch has still not been uploaded. `[verified 2026-08-31 from the repo's own stored snapshots]` **8/29 `11:08:07Z props-batter us2 73` · 8/30 `11:09:52Z props-batter us2 70`, against ~140 for a full two-region 14-game batter pull, while `props-pitcher` took `us,us2` two seconds earlier both days.** ➡️ **The 8/29, 8/30 and 8/31 cards all price their hitter rows off Hard Rock alone.** **The zip Sam was sent on 8/30 is the fix and it is still not on main.**
  - **`card.py`'s `in_band` ceiling (2.1x) contradicts its own `parlay_rule` prose (2.2x)** — unchanged since 8/26 and re-reported by every grading run since.
  - **`card.py:729` and `verify_card.py:834` still exclude a rung priced at EXACTLY −700** (`price > -700` / `a <= -700`) where Sam's rule is "−700, nothing shorter".
  - **`card.py`'s `calibration_warning` is still a grading run stale.**
  - **`.github/workflows/collect.yml`'s own header says "the count is 16" where the file enumerates SIX cron entries.**
- ✅ **DOC DEBT CLEARED AND VERIFIED AT THE SOURCE: BOTH 2026-08-24 task-prompt debts are DONE.** **The Monday sweep prompt now names no region set and points at rule 48; the 7:30am grading prompt now points at `.github/workflows/collect.yml` and states no times.** ⛔ **NEW task-prompt debt was filed in `claude/owed-tests.md` DOC DEBT rather than fixed — `update_trigger` requires human approval and an unattended run has nobody to give it.**
- ⚠️ **Method:** every doc edited by `project_read` → **the tool result extracted VERBATIM from this session's transcript JSONL with a JSON walker, never retyped** → programmatic patch with anchors ASSERTED to occur EXACTLY ONCE before any write → `collections.Counter` line-loss check → fresh `project_read` and `created_at` compared immediately before upload → upload with `local_path`, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT, NO GRADED ROW, NO MONEY ROW, NO DATED CARD TABLE, NO CALIBRATION FIGURE AND NO BREAK-EVEN VALUE WAS ALTERED IN ANY DOC.**

### v4.9 — Aug 24 2026, Monday sweep (the 8/23 widening reached the code and the ledger but not the doc set — a LIVE `regions=us2` instruction survived in three docs for a day)

- 🔴 **THE v4.8 SWEEP THIS MORNING FIXED LEDGER RULE 48 AND THIS FILE AND THEN STOPPED. IT DID NOT SWEEP DOWNSTREAM.** **Three docs were still issuing a LIVE `regions=us2`-only instruction, together with one-region credit figures, on 2026-08-24: `claude/card-blueprint.md` (STEP 5), `claude/mlb-data-stack.md` (FIVE separate places) and `claude/gizmos-picks-spec.md`.** ✅ **All are struck and restated at `regions=us,us2`, each pointing at `claude/pick-ledger.md` rule 48 as the OWNER of the current region/book set.** ➡️ 🔴 **THE LESSON, RECORDED AS A STANDING RULE: AN ENUMERATED LIST OF INSTANCES IS NOT A SWEEP.** **v4.8 named the places it fixed inside this file and read as complete. After a region or market change, GREP EVERY DOC FOR THE OLD STRING** — the enumeration is the report, never the method.
- 🔴 **AND THE FAILURE MODE INVERTED, SO BOTH DIRECTIONS ARE NOW ON THE RECORD.** **Ledger rule 61 was a rule live in the DOCS that never reached the CODE. This was a rule live in the CODE — and in the repo's own `CLAUDE.md` — that never reached the DOCS.** ⛔ **CHECK BOTH WAYS, EVERY WEEK. A one-directional consistency sweep will keep missing half of them.**
- ✅ **OTHER CONTRADICTIONS FOUND AND FIXED, ONE LINE EACH:**
  - **`claude/mlb-alt-line-protocol.md`** treated **2.1x as a hard CEILING**; it is a **SOFT TARGET** — Sam, 8/22: *"if its a good bet its a good bet still show it"* — and it said **"five of seven carded plays"** where the 8/21 card is **EIGHT**.
  - **`claude/mlb-opponent-database.md`** printed its LIVE formula centred on the **RETIRED fit-sample constant** while instructing the reader to use the live one — 🔴 **this project's oldest documented bug reproduced INSIDE THE DOC THAT OWNS THE CONSTANT.**
  - **`claude/times-through-order.md`** called Toronto *"dead mid-pack, so the database says nothing either way"* off a **retired-scale value**, when Toronto is **5th-HARDEST on the live scale** — 🔴 **the CONCLUSION, not just the number, was false.**
  - **`claude/mlb-team-advanced.md`** hard-coded the live centering constant **four lines below its own ban on doing so**, and called parks/domes/weather a **measured null**.
  - **`claude/pitcher-tier-finding.md`** still listed **HITS ALLOWED as an owed re-run** when it was re-run on the full sample.
  - **`claude/backtest-weather-homeaway.md`** restated the struck **"hits is the market with no model."**
  - **`claude/data-schedule-join.csv.md`** still carried an **un-struck LIVE join instruction** inside a doc banner-marked **DO NOT JOIN AGAINST IT**.
  - **`claude/mlb-data-stack.md`** told the reader to **paste the API key into a project doc**.
  - **`claude/calibration-accumulators.md`**'s rule-50 progress line said **9 instances over 11 rows**.
  - **`claude/today-shortlist.md`** asserted an **ungraded 8/21 card** that was **graded 6/8 on 8/22**.
- ⚠️ 🔴 **REPORTED, NOT FIXED — A HEADLESS RUN CANNOT TEST OR PUSH CODE** (`github.com/smh0602/gizmos-picks`):
  - **`card.py:421` and `verify_card.py:179` both write `price > -700`**, which EXCLUDES a rung priced at **EXACTLY −700** — Sam's rule is **"−700, nothing shorter"**, so −700 itself is acceptable.
  - **`card.py`'s calibration band notes are a grading run stale.**
  - **`collect.yml`'s evening-cycle comments still describe a 21:40Z/21:45Z cycle the crons moved to 20:10Z/20:35Z/20:50Z.**
  - **`collect.yml`'s header says "31 runs a day … ~930 of the 2,000 free minutes"** where the crons actually enumerate **43 runs/day ≈ 1,290 minutes/month**.
  - **`collect.py`'s batter-props branch carries a comment saying *"one book is enough … fifteen is paying for precision"* directly above an assignment of `REGIONS_FULL`** (both regions), and **`REGIONS_CHEAP` is now an unused constant**.
  - **The repo's `CLAUDE.md` says `verify_card.py` has 26 checks where it has 27.**
- ✅ **JOB 1, THE PITCHER DATABASE — THE PUBLISHED TABLE VERIFIED: 185 rows, `sum(n)` 3,403, ZERO rows below n≥8, and both named rebuild acceptance-test pitchers reproduce.** ⚠️ 🔴 **IT IS BUILT THROUGH THE 8/20 SLATE AND IS THREE COMPLETED SLATES BEHIND: 8/21, 8/22 and 8/23 were 15 games each, so 90 STARTS ARE MISSING** `[derived from enumerated game counts in the repo's own append-only snapshots]`. 🔴 **A REBUILD NEEDS CHROME — an interactive session must run THE RECIPE.** ⚠️ **And `statsapi.mlb.com` is NOT REACHABLE FROM A SCHEDULED CLOUD RUN AT ALL (`CONNECT tunnel failed, 403`), so the season population could not be fetched directly and was BOUNDED FROM THE REPO instead.**
- ⏳ **STILL OPEN FOR SAM, CARRIED FORWARD FROM v4.8 AND NOT GUESSED: does "CARD ONLY PRIZEPICKS AND HARD ROCK" widen with the five-book set?**
- 🔴 ⛔ **NOT FIXED — AND THE FIRST DRAFT OF THIS ENTRY CLAIMED IT WAS. Corrected in the same turn (check 25).** **`update_trigger` returns `MCP tool call requires approval`, and a scheduled run has no human to approve it, so NO scheduled-task prompt can be edited from an unattended session at all.** **Both stored prompts were re-read and byte-compared afterwards: UNCHANGED.**
- 🔴 **THE LIVE HAZARD, STATED PLAINLY: the Monday sweep's OWN prompt still declares `regions=us2` the standing pull and instructs the run to FLAG every doc that says `regions=us,us2`.** **Left as written, the sweep firing Monday 2026-08-31 will strike `us,us2` out of every doc and re-narrow the entire project back to Hard Rock only — the exact opposite of Sam's 8/23 instruction, and it will do it to the docs this sweep just repaired.** ➡️ **AN INTERACTIVE SESSION MUST EDIT THAT PROMPT BEFORE 2026-08-31.** **The exact replacement text is recorded in `claude/owed-tests.md` DOC DEBT.**
- ⚠️ **SECOND, SMALLER: the 7:30am grading prompt hard-codes the machine card's write times as `14:45Z and 21:50Z`; the evening cron is `20:50Z`.** **The fix is this project's own — delete the times, point at `.github/workflows/collect.yml`.**
- 🔴 **AND THE STRUCTURAL LESSON, WHICH IS BIGGER THAN EITHER PROMPT: A SCHEDULED TASK CANNOT REPAIR A SCHEDULED TASK.** **Every tripwire in this project that says “check every trigger” can only ever be REPORTED by an unattended run, never fixed by one.** ➡️ **Task-prompt debt must therefore be written into a DOC, where an unattended run CAN write, and picked up interactively.**
- ⚠️ **Method:** every doc edited by `project_read` → **verbatim local copy** → **programmatic patch with uniqueness-asserted anchors** → **`collections.Counter` line-loss check** → **re-read and `created_at` compared immediately before upload** → **upload with `local_path`**, per ledger rule 43. ⛔ **NO MODEL COEFFICIENT VALUE WAS ALTERED ANYWHERE — several were RELOCATED behind pointers to `claude/mlb-projection-model.md`. No graded row, money row, dated card table or data block was touched in any doc.**

### v4.8 — Aug 24 2026, Monday sweep (rule 48 was superseded in the CODE a day before the docs heard about it — five books, two regions)

- 🔴 **SAM WIDENED THE ODDS SOURCE ON 2026-08-23 EVENING, AND THE INSTRUCTION REACHED THE CODE BUT NOT THE DOCS.** **As recorded in `github.com/smh0602/gizmos-picks` `CLAUDE.md` and commit `91b0290` (authored on Sam's account, 8/23 22:27 ET):** *"lets just only use the top 5 sportsbooks in the USA, hardrock, draftkings, fanduel, ceasars, and bet MGM"* ➡️ **The standing set is FIVE BOOKS — Hard Rock (`hardrockbet`/`hardrockbet_oh`), DraftKings, FanDuel, Caesars (`williamhill_us`), BetMGM — and the props pull is `regions=us,us2` again, because four of the five live in `us`.** ⛔ **`us_dfs` stays banned; PrizePicks is still never a source of a number (rule 46 / checks 31–32 unchanged).** 💰 **Per-game prop cost doubles back to 2× markets; the collector's props schedule was cut from three pulls a day to two to pay for it — budget arithmetic in `.github/workflows/collect.yml`, plan is the $30 / 20,000-credit subscription (`claude/gizmos-picks-spec.md`).**
- 🔴 **THIS IS RULE 61's FAILURE MODE INVERTED — a rule live in the CODE that was not yet a rule in the DOCS.** **Found only by reading `collect.py` (`REGIONS_FULL` on the props pull, the `BOOKS` dict) and the repo's `CLAUDE.md`; no project doc knew.** **Swept in this file: check 4, check 33, the Coverage paragraph, the book-coverage cost line and the alt-outs consequence section all carry a dated WIDENED note pointing at ledger rule 48 as the owner — the region set has now changed three times in three days, which is exactly why the current set must be READ FROM RULE 48 and never restated bare.**
- ⏳ **LEFT OPEN FOR SAM, NOT GUESSED: does "CARD ONLY PRIZEPICKS AND HARD ROCK" widen with the five-book set?** The repo's "the dashboard only shows books Sam can bet at" reads as though he can bet all five; nothing records him saying so. **Until he answers: card the two, pairs Hard Rock-only (code-enforced), a play priced only at DK/FD/Caesars/MGM is shown with its book named.**
- ⚠️ **Also noted by the same sweep, REPORTED NOT FIXED (a headless run cannot test or push code): `card.py`'s calibration band notes are one grading run stale (60–70% now −29.6 not −25.9; 70–80% now −1.3 at n=23 and no longer "the band that supports the number"; 80%+ now −14.7 not −18.0 — live figures in `claude/pick-ledger.md`); `card.py`/`verify_card.py` treat a price of EXACTLY −700 as below Sam's floor (`price > -700`) where his rule is "−700, nothing shorter", i.e. −700 itself is acceptable; and `collect.yml`'s comments still describe a 21:40Z/21:45Z/21:50Z evening cycle the crons have moved to 20:10Z/20:35Z/20:50Z.**
- ⚠️ **Merged, not rewritten: two INDEPENDENT transcriptions of the current doc (one by this session, one by a subagent from its own fresh `project_read`) compared byte-identical (66,760 bytes) → programmatic patch with uniqueness-asserted anchors → `collections.Counter` line-loss check (zero lines lost) → re-read and `created_at` compared immediately before upload → upload with `local_path`, per ledger rule 43.** ⛔ **No model number, no framework step, no banned argument, no break-even value and no graded figure was altered. Dated revision-log prose below is history and is left exactly as written.**

### v4.7 — Aug 22 2026 (Hard Rock only, and the card is now half price)

- 🔴 **SAM NARROWED THE ODDS SOURCE, IN HIS OWN WORDS:** *"i onlt want to see hardrock odds from now on for all props"* ➡️ **This SUPERSEDES AND TIGHTENS the v4.5 instruction.** ~~`regions=us,us2`~~ **is struck; the standing pull is `regions=us2` — HARD ROCK ONLY, EVERY PROP, EVERY CARD.** **Hard Rock lives in `us2` only, so one region is correct and sufficient.** ⛔ **`us_dfs` was already banned and stays banned.** ➡️ **PRE-PUBLISH CHECK 33 amended; ledger rule 48 amended in place.**
- 💰 **THE CARD IS NOW HALF PRICE.** `[measured 2026-08-22]` **The Odds API bills `cost = unique markets × regions, per game` — confirmed by a probe returning `x-requests-last: 1` for one market on one region.** **OLD: 4 markets × 2 regions = 8 credits per game. NEW: 4 markets × 1 region = 4 credits per game.**
- ✅ **THE CROSS-BOOK CHECK SURVIVES, AND IT SURVIVES FOR FREE — WHICH IS WHY THIS COSTS NOTHING IN SAFETY.** `[measured 2026-08-22]` **A `us2` pull still returns several books in the same response at no extra cost: a probe returned `hardrockbet`, `espnbet` and `fliff` on one market, and `hardrockbet_oh`, `ballybet` and `betparx` also live in that region.** ➡️ **Check 32 / ledger rule 47 — the distinct-price sanity check — therefore still has a comparison set in every single pull.**
- ⚠️ **WHAT IS GIVEN UP, SAID HONESTLY: price shopping against DraftKings, FanDuel, BetMGM, BetRivers, Bovada and BetOnline (`us`).** ✅ **It was never actionable. Sam bets Hard Rock and PrizePicks only, so a better DraftKings price was information he could not use.** ⛔ 🔴 **THE RESIDUAL RISK IS RECORDED RATHER THAN WAVED OFF: a Hard Rock price offside versus the wider market is no longer visible against the `us` books. The `us2` siblings are the remaining check.**
- 🔴 **CREDIT POSITION AT THE TIME OF WRITING: 60 remaining. At 4 credits per game that is 15 games — exactly one full slate.** **Recorded in `claude/mlb-data-stack.md`.**
- ✅ **SWEPT FOR CONSISTENCY IN THIS FILE, because leaving them would have instructed a future session to pull `us`:** **check 4**, the **book-coverage cost line**, the **Coverage — no silent narrowing** paragraph, and the **three `us,us2` references in the alt-outs consequence section** are all struck and restated at `regions=us2`. ⛔ **Dated revision-log prose below is history and is left exactly as written.**
- ⚠️ **PrizePicks is unchanged by this: still a place Sam BETS, still never a source of a NUMBER (ledger rule 46 / check 31).**
- ⚠️ **Merged, not rewritten: `project_read` → local file → programmatic patch with uniqueness-asserted anchors → `collections.Counter` line-loss check → upload with `local_path`.** ⛔ **No model number, no framework step, no banned argument, no break-even value and no other pre-publish check was altered.**

### v4.6 — Aug 21 2026, evening (two standing rules with no enforcement, and an audit that found the docs asserting fixes that had never landed)

- 🆕 🔴 **LEDGER RULE 50 — WHEN THE PITCHER'S PROFILE AND THE LINEUP'S PROFILE CONTRADICT, GO TO THE DIRECT HEAD-TO-HEAD.** Sam's rule, in his words: *"when you see 2 contradictions, a pitcher who doesn't strike out batters and a lineup who strikes out a ton, i want you to look at the matchups between the two… we only look at this season and the last season to get the most recent stats on the matter."* ➡️ **A contradiction is the TRIGGER, not a routine step. It is a TIEBREAKER, never an input, never folded into the blend, and `n` is PRINTED on the row INCLUDING `n=0`.** `[measured 8/21]` **four of the ~~ten carded arms~~ TEN ARMS CHECKED had never faced that night's opponent in 2025–26 — "none" is a finding that gets printed.** *(wording corrected 8/21 late; ledger rule 50 / rule 45)* ✅ **Its first run resolved the contradiction it was invented for:** E. Rodríguez (6.4 K/9, lowest on the slate) against Cincinnati (the most strikeout-prone lineup in baseball) — **3 K and 3 K across both meetings, the second while being knocked out in the third on 85 pitches.** 🔴 **AND IT CUT AGAINST CLAUDE TWICE ON THAT SAME RUN, WHICH IS WHY IT IS WORTH HAVING** — Schlittler had gone **7 K, 7 K, 7 K** against Toronto in 2026 against a matched class of 18/29 that had been used to talk Sam off him, and Sonny Gray took **6 K and 7 K** off San Francisco after being called "the weakest of the six." ⚠️ **Rankings deliberately NOT changed mid-card — re-weighting evidence the moment it says something better is this project's documented failure mode.** ➡️ **PRE-PUBLISH CHECK 35.**
- 🆕 🔴 **LEDGER RULE 51 — A REMATCH INSIDE 30 DAYS HAIRCUTS THE HIGH THRESHOLDS AND BARELY MOVES THE LOW ONES.** Prompted by Sam: *"your not worried about dobbins facing the phillies 2 starts ago."* **296 pairs this season, mean gap 15.2 days.** ➡️ **A rematch is a reason to DROP A RUNG, not to drop the pitcher.** ➡️ **PRE-PUBLISH CHECK 36.**
- 🔴 **AND ITS EPISTEMICS ARE THE POINT, SO THEY ARE RECORDED BEFORE THE NUMBERS ARE.** ⚠️ **THE PRE-REGISTERED SPECIFICATION FAILED:** mean K **5.04 → 4.74**, a drop of **0.30**, **t = −1.78**, against a bar of ≥0.5 and |t| ≥ 2.0. **FAILED, and recorded as CLOSED-FAILED.** 🔴 **A THRESHOLD ANALYSIS WAS THEN RUN ON THE SAME DATA AND IS SIGNIFICANT — AND IT IS EXPLORATORY:** 4+ K fell **221/296 → 182/296**, paired McNemar **χ² = 12.78** (76 hit-then-miss against 37 miss-then-hit); conditional on a strong (≥5 K) first meeting the next start reaches 4+ K **1363/1826 = 74.6%** against anyone but only **116/173 = 67.1%** against the same team again — while at a 3+ K bar the split is merely **87.5% → 84.4%.** ⛔ **Looking a second way after the primary failed IS specification shopping — this project's own documented worst error — and the exploratory result may NEVER be silently promoted to the registered one.** ✅ **Registered as owed test T18 with the FAILURE RECORDED FIRST, and a REPLACEMENT specification pre-registered for the September re-fit.**
- 🔴 **THE PROJECT-WIDE AUDIT THAT FOLLOWED, AND IT FOUND MORE THAN THE TWO RULES DID.** Across the doc set: **a FALSE CLAIM OF FULL-SEASON VERIFICATION** for the team-record null; **live card instructions whose derivations had been struck**; **retired-scale values still driving live conclusions** — including one verdict that flipped outright and one ordering that reversed; **several unreconcilable counts**; **dangling pointers to a deleted doc**; and 🔴 **a register that did not contain a test another doc claimed was registered in it** — `claude/pick-ledger.md` rule 51 stated its specification was *"written into `claude/owed-tests.md`"* and it was not there at all. **That is check 25 exactly: a doc asserting a fix is not a fix.** ✅ **THREE REGISTRATIONS WERE OWED AND ARE NOW IN THE REGISTER:** **T18** the rematch test (closed-failed, with its replacement spec), **T19** the PrizePicks DEMON/GOBLIN tag test — which had been living **only inside a card write-up**, the exact failure the register exists to prevent — and **T20** a full-season re-test of **COUNT form versus RATE form for the strikeout model**, a **LIVE model-form decision resting on 699 rows of the 40%-complete log** and in no entry anywhere.
- 🔴 **ALL FOUR SCHEDULED TASK PROMPTS REWRITTEN**, verified against `list_triggers` in the same turn. 🆕 **The grading task was the serious one: as written it would have MIS-GRADED SAM'S REAL-MONEY SLIPS AS INDEPENDENT PLAYS RATHER THAN ALL-OR-NOTHING TICKETS.** ✅ **It now grades a TABLE C row as a SLIP** — every leg must hit or the slip returns **$0** — **computes `returned = stake × multiplier`, appends a 💰 BANKROLL row, restates staked / returned / profit / ROI, leaves a split-day ticket wholly pending rather than part-grading it, and ⛔ never attaches a break-even or an EV to a PrizePicks leg.** ⚠️ **It also now excludes an OUT-OF-BAND slip from any hit rate the band is measured on, and says which were excluded — or the band measures nothing.**
- 🟣 **SAM'S TWO PRIZEPICKS SLIPS LOGGED IN TABLE C, from his own screenshots at 5:41pm** — **Manaea o3.5 K + E. Rodríguez u5.5 K at 1.80x for $30**, and **Dobbins o3.5 K (DEMON) + Melton o2.5 K at 2.20x for $20.** **Four pitchers, four games, no shared leg** — **his restructure, and an improvement on what was recommended**; every draft that reached him carried Rodríguez on both tickets, worth ~30% chance of losing the whole stake against ~12%. ⚠️ **Slip 2 is OUT OF BAND at 2.20x and must be counted as out-of-band when the band's hit rate is measured.** 🔴 **TABLE C was WRONG TWICE before the screenshots arrived, because recommendations were written into a bet table** ➡️ **PRE-PUBLISH CHECK 37.**
- 🔴 **THE THREE-REGION COST LINE STRUCK — IT CONTRADICTED THIS DOC'S OWN CHECK 33.** ✅ **Two regions, `regions=us,us2`; check 4 corrected to match.** 🆕 **AND WHAT RULE 48 DOES TO THE MANDATORY ALT-OUTS PULL IS NOW WRITTEN DOWN, WHICH NOBODY HAD DONE:** **Hard Rock posts no alternate outs market at all and PrizePicks was the only book in the pull that carried one**, so **`pitcher_outs_alternate` under `us,us2` may return nothing.** ✅ **The pull stays MANDATORY, an empty result is a FINDING reported with the market key, the regions and the row count, and ⛔ a DFS rung may NOT be substituted.**
- 🔴 **CHECK 28's OWN COUNT WAS WRONG AND IS CORRECTED — three → FIVE.** **A check that miscounts is the defect it exists to catch.**
- ✅ **Doc list and scheduled-task table re-verified against `project_info` and `list_triggers` in this same turn (check 10).** ⛔ **No doc count and no rule count is written into this file.** **Name the doc, never the tally.**

### v4.5 — Aug 21 2026, 4:15pm (the odds source is now fixed at the request, and Hard Rock's feed is confirmed against Hard Rock's app)

- 🔴 **NEW STANDING INSTRUCTION FROM SAM — ledger rule 48, pre-publish check 33:** *"from now on i want you to pull all odds from sportsbooks not prizepicks, i see the pattern of you messing up is when you are pulling odds from alt lines on prizepicks. to avoid this let's just pull our odds from hardrock and other sports books."* ➡️ **Pull `regions=us,us2`. Never `us_dfs`.** **The fix is at the REQUEST, not the write-up.**
- ⚠️ **PrizePicks is not banned as a place to BET — Sam bets there. It is banned as a source of a NUMBER.**
- ✅ 🔴 **HIS DIAGNOSIS IS CORRECT ON THE EVIDENCE.** Every price error that reached him on 8/21 — Burke 3.5, Gore 4.5, Detmers 4.5, Misiorowski — came from PrizePicks alt rungs, and **not one Hard Rock quote on the same cards was wrong.**
- 🆕 ✅ **FIRST END-TO-END CONFIRMATION IN THE PROJECT THAT A FEED PRICE EQUALS A BOOK PRICE.** Sam sent Hard Rock's Sean Burke alt-strikeout ladder; **all NINE rungs match the raw pull to the dollar.** ⚠️ **With a label offset that is a live hazard: the app writes "To Record 4+ Strikeouts" where the feed writes "Over 3.5".** ➡️ **Ledger rule 49 / check 34 — quote both forms.** ✅ **Sam's correction confirmed exactly as stated: o3.5 = −1000.**
- ⏱️ 🔴 **PRICES ARE NOW TIME-STAMPED.** Misiorowski's o6.5 read **−550** at ~3:00pm and **−850** in his app at 3:32pm; the fresh pull reads −850. ➡️ **Line movement, not a feed error — and a card built at 3pm may not assert an 8pm price.**
- 🟡 **`claude/pick-ledger.md` gains TABLE A2 — the 8/21 card re-priced on Hard Rock — recorded as PAPER, in no denominator, with the reason stated BEFORE the results were known.** 🔴 **Five of TABLE A's eight rows do not survive real pricing, and Yamamoto's o17.5 outs rung does not exist on Hard Rock at all.** ⛔ **TABLE A is unchanged and graded as logged.**
- 🆕 ✅ **T16 PRE-REGISTERED AND PASSED** — a BAD-tier arm (K/9 < 9.0, ERA > 3.50) against a **top-10 offense** goes under 15.5 outs **54.1% (304/562)** against **47.5% (539/1134)** elsewhere: **+6.6 points, z = 2.54, n = 1,696.** **Sam's structural observation, measured.** ⚠️ **It measures the BASE RATE only — the price half is T17 and is blocked on LINE VALUE (n=0).** ⛔ **Do not quote T16 as evidence of an edge against a price:** the first live application, Chandler u15.5 at −160, has an edge of **−6.9.**

### v4.4 — Aug 21 2026, evening (the project's stated premise was reading an enum as a price)

- 🔴 **`−137` IS NOT A PRIZEPICKS PRICE. IT IS THE ODDS API'S ENCODING FOR "GOBLIN"; `+100` ENCODES "DEMON."** `[measured 2026-08-21]` Across **7 games × 4 markets** PrizePicks returned **two distinct values in 145 outcomes — −137 (×77) and +100 (×68)** — while the same pull returned **80 distinct prices from hardrockbet, 73 from espnbet, 28 betparx, 27 underdog, 23 fliff, 18 ballybet, 1 pick6.** **No sportsbook prices 145 props at two numbers.** **Sam caught it from the app:** *"sean burkes 3.5 is not priced at −137… you can't keep giving me false info."*
- ⛔ **CONSEQUENCE: every break-even, edge and EV ever computed on a PrizePicks play is arithmetic on a constant and is MEANINGLESS, not merely imprecise.** On the 8/21 card that voids the quoted edges of **+27.6, +19.1, +18.2, +18.1 and +12.4 — five of the ~~seven~~ EIGHT carded plays.** *(denominator corrected 8/21 late — the card is EIGHT plays, five of them PrizePicks rows.)*
- 🔴 **STRUCK: the "Nola −750 on Hard Rock vs −137 on PrizePicks" comparison that THE GOAL, PRECISELY used as the project's whole thesis.** One side was a quote; the other was a label.
- 🔴 **STRUCK from the break-even table: the `−137 57.8%` entry.** It existed to price goblins.
- ✅ **WHAT SURVIVES, precisely: the model probabilities, `E[K]`, `mu`, the raw rates and the matched-class check are UNTOUCHED. Every Hard Rock price, edge, break-even, de-vig and pair multiplier STANDS** — Hard Rock returned 80 distinct values in the same pull.
- ✅ **"At a flat payout, take the lowest rung that clears" SURVIVES — with a NEW JUSTIFICATION.** Not "PrizePicks charges −137 everywhere" but **"the 2-pick multiplier barely moves with rung difficulty"** — Sam's real slips returned **1.8x, 1.9x, 2.0x.** **A harder rung for a near-identical multiplier is still a donation.**
- ✅ **The DFS thesis survives in WEAKENED form:** PrizePicks' payout structure is insensitive to rung difficulty in a way a sportsbook's is not. ⚠️ **It can no longer be quantified from the feed, and it was never measured — it was inferred from an encoding.**
- 🆕 🔴 **TWO STANDING RULES ADDED to BET CONSTRUCTION** — (a) a PrizePicks play may be ranked by probability but **never carries a break-even, an edge or an EV**; quote `model / raw / blend / matched class` and **ask Sam for the in-app multiplier**; (b) **count a book's DISTINCT PRICE VALUES before treating its prices as real** — 1–2 across dozens of outcomes means labels.
- 🆕 **PRE-PUBLISH CHECKS 31 and 32 added** for those two rules. **Ledger rules 46 and 47 mirror them.**
- ⚠️ **FIFTH INSTANCE of a fact about a QUERY written down as a fact about the WORLD**, after the two Hard Rock ladder errors, WebFetch's 27 fabricated events, and the feed omitting Luzardo's whole market. 🔴 **The worst of the five: load-bearing for the stated premise, and it survived every audit for days.**
- ⚠️ **OPEN, NOT RESOLVED: Sam says Burke's 3.5 rung "is not priced at −137." That confirms the price is fake and says NOTHING about whether the 3.5 rung exists in the app.** ⛔ **Recorded as a question for Sam; the rung is asserted in neither direction.**
- ⚠️ **The −700 alt-rung floor is now explicitly a HARD ROCK-ONLY filter** — a PrizePicks rung has no price for the floor to test.

### v4.3 — Aug 21 2026, 7:30am (the "safe" headless path fabricated a number)

- 🔴 **THE FLAT `gameLog` ENDPOINT IS NOT SAFE ON ITS OWN.** The stack doc says it is *"a shape a summarising fetch cannot merge."* **True about blocks, false about scalars.** A pull returned **`1.3` innings pitched — a value that cannot exist** — and it was caught only because the full game log summed **2 outs short** of the season total. ✅ **No graded line was affected** (relief appearance, wrong date, K column clean). ➡️ **Pre-publish check 27 and ledger rule 40: domain-check every fetched value.** ➡️ **And the stronger control is to sum the WHOLE log against the season total, not to check one delta.**
- 🔴 **RULE 15 HAS NEVER BEEN EXECUTABLE.** Every card to date logged only the **blend**, not the model number and the raw rate it is made of. The 8/20 scoreboard had to be **reconstructed from first principles** — it validated to ≤0.2 points on 9 of 11 plays, **but a reconstruction is not a log.** ➡️ **Pre-publish check 26: three probability columns.**
- 🔴 **THE BLEND WEIGHT IS UNIDENTIFIABLE AT CURRENT n.** Brier across w=0 to w=1 spans **0.0011** at n=12. **The "which was closer" tally the rule originally specified is win-rate-biased and is the wrong statistic.** ➡️ **Opened owed test T15 — adopt a fitted w only at n≥100 and only with a 0.005 Brier margin.** ⛔ **The 50/50 stands.**
- 🆕 **`claude/calibration-accumulators.md` created** and added to the doc list — the rule-15 scoreboard and the first **itemised** rule-35 shadow ladder (**83 rungs across 12 NAMED starts**). 🔴 **The previous shadow pass recorded bucket counts and no start list, so it could not be deduplicated and could not be accumulated into. Superseded.**
- ✅ **OPPONENT TABLE VERIFIED — NO-OP.** Population **3,838** confirmed **three independent ways**, including a new per-team check against the standings endpoint that validates **all 30 rows** rather than just the total. League-mean drift **+0.0009**.
- 🔴 **NEW WORKING-AGREEMENT RULE: verify by CONSISTENCY, not EQUALITY, against rounded published values.** Five correct Δ rows were flagged by a naive recompute.
- 🔴 **Ledger labelling audit: an orphaned "Table B" reference with no Table B in the document.** ⛔ **Flagged, not guessed at.** **Recovered ledger rules 38 and 39, referenced in the changelog for a day and never added to the list.**
- ✅ **G. Rodriguez's 8/20 loss upgraded from `[Sam's word]` to `[measured]`.** **Verify Sam-reported results at the next grading run.**

### v4.2 — Aug 21 2026 (the automation was auditing itself against tasks that did not exist)

- 🔴 **THE LINE-CAPTURE TASKS DO NOT EXIST.** Verified against `list_triggers`. **LINE VALUE is interactive-only.**
- 🔴 **ALL FOUR REAL TASKS REWRITTEN.** Three carried pre-v4.0 instructions that would have caused damage.
- 🔴 **Check 25 and the working-agreement rule: a doc asserting a fix is not a fix.**
- 🔴 **THE INLINE MODEL SUMMARY IS DELETED FROM THIS FILE.** It had drifted twice in two days.
- 🆕 **`claude/owed-tests.md` created** — eleven pre-registered tests plus DOC DEBT.
- 🆕 **Rules 35–37 in the ledger.**
- 🔴 **`cv` uses the SAMPLE SD (n−1).** A population-SD rebuild was wrong in all 185 rows.
- ✅ **Both databases refreshed through 8/20** on **3,838 starts**; 12 aggregate checksums matched.
- ✅ **8/20 card graded 8/10** against a 78.4% estimate. **70–80% well calibrated at n=14 (+3.2); 60–70% broken at −22.5.** ➡️ **raise the carding floor, do not haircut** — owed test T4.
- 🔴 **The #1 play lost for a reason the model cannot see** — owed test **T1**.
- 🔴 **DATA LOSS FOUND AND RECORDED: TABLE B — the 8/19 midday `[pre-correction]` card, 5 plays, 4/5 — was deleted by a full-file rewrite and left only a dangling reference.** Sam asked for it to be recorded as lost rather than reconstructed. **Cause: `project_write` replaces the whole doc with no diff, combined with a standing instruction to prune.** Ledger rules 42–44 and checks 28–30 exist because of it. ✅ **Two further count mismatches surfaced by the new table index were resolved the same day and were NOT data loss.**

### v4.1 — Aug 20 2026, evening (the payout band; a team flip; a substring bug)

- 🔴 **The 1.8x–2.1x band became a hard constraint**, with the arithmetic that retires an entire class of pair.
- 🔴 **Ladder direction corrected** — overs get safer going down, unders going up.
- 🔴 ~~**Hard Rock alt ladders are OVERS-ONLY**~~ **— FALSE, corrected by Sam 8/21.** ✅ **The "no outs alternate at all" half survives.**
- 🔴 **Team-assignment failure** worth +0.79 K on one arm. **Check 22.**
- 🔴 **`'pitcher_strikeouts'.includes('outs')` is true.** **Check 23.**
- ⚠️ **20 in-band pairs existed and only 3 were +EV.** **Fewer pairs, stated plainly, beats filling a quota.**

### v4.0 — Aug 20 2026 (the dataset was 40% complete; every coefficient was wrong)

- 🔴 **The archive held 1,517 starts against a true season of 3,820.** **Every coefficient had been fitted on it.**
- 🆕 **Full backfill in seconds, zero credits**, via statsapi game logs read same-origin in Chrome.
- 🔴 **ATTENUATION BIAS was the mechanism.**
- 🔴 **STRUCK: previous start's OUTS.** **The right variable is PITCH COUNT**, and the direction is **persistence**.
- 🔴 **STRUCK: the opponent term in the OUTS model.**
- ✅ **Handedness re-tested and still null.** ⚠️ Rest days significant but negative and confounded.
- 🆕 **Pitcher database rebuilt at true n≥8.** 🔴 **An argument about measurement error was illustrated with measurement error.**
- ⚠️ **NOT re-tested on the full sample: parks, weather, hits allowed, team record** — owed test T13.
- ⚠️ **Baseball Savant is NOT blocked.** **StatMuse truncated the same population query by 83% with no warning.**

### v3.5 and earlier — condensed

- **v3.5** — **STRUCK "two −137 goblins cannot clear 1.8x."** **ASK the in-app multiplier.** **A won bet is not a correctly priced bet.** First money logged.
- **v3.4** — previous-start outs added (**struck in v4.0**).
- **v3.3** — ledger rebuilt to one table per card; the website reframe; the first ten pre-publish checks.
- **v3.2** — opponent and home/road made mandatory.
- **v3.1** — Sam audits the inputs; per-pitcher home/road splits banned.
- **v3.0** — the raw-hit-rate check added mid-card, killing three plays on first use. Hard Rock found in `us2`.
- **v2.9** — **"right about the spot, wrong about the reason"** enters the working agreement.
- **v2.8** — the pitcher database built, after Sam asked why it did not already exist.
- **v2.7** — weekly doc sweep added; two live contradictions found immediately.
- **v2.6** — strikeout tier-split struck as a peeking artifact; weather and parks null.
- **v2.5** — the opponent-metric bug; BET CONSTRUCTION added.
- **v2.0–2.4** — %6+ suppression falsified; hits allowed null both directions; the goal stated precisely.

**8/18 card: 7/11 (63.6%).** Wheeler o4.5 K + Harrison o3.5 K at 1.9x, both cashed. **$30 → $57.00.**
