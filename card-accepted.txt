# CARD FAILURES SAM HAS LOOKED AT AND CHOSEN TO LIVE WITH.
#
# 🔴 ONLY SAM ADDS A LINE HERE. Claude does not, ever. Putting a check on
# this list does NOT move its bar, does NOT publish a card that failed it,
# and does NOT make the problem go away -- the card is still refused and
# the page still says so. The ONLY thing it changes is that the run stops
# going red every fifteen minutes for a decision already taken.
#
# ⛔ REMOVE A LINE THE MOMENT THE UNDERLYING PROBLEM IS FIXED. A stale
# entry here is a check that silently stopped mattering.
#
# format:  CHECK_ID | why, and the date the decision was made
#
T37 | 2026-08-29, Sam: "leave t37". Hitter projections contradict the market on ~5.4% of 70%+ rows against a pre-registered 5.0% bar. The bar was NOT moved -- moving a pre-registered bar after seeing the data is specification shopping. The card stays blocked until the projections improve. WATCH: if this is still failing after several days it is the model, not one slate.
